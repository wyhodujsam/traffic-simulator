---
phase: 2
plan: 1
name: "Domain Model Classes"
wave: 1
depends_on: []
requirements: ["ROAD-01", "SIM-02"]
files_modified: ["backend/src/main/java/com/trafficsimulator/model/Vehicle.java", "backend/src/main/java/com/trafficsimulator/model/Lane.java", "backend/src/main/java/com/trafficsimulator/model/Road.java", "backend/src/main/java/com/trafficsimulator/model/RoadNetwork.java", "backend/src/main/java/com/trafficsimulator/model/Intersection.java", "backend/src/main/java/com/trafficsimulator/model/IntersectionType.java", "backend/src/main/java/com/trafficsimulator/model/SpawnPoint.java", "backend/src/main/java/com/trafficsimulator/model/DespawnPoint.java"]
autonomous: true
---

# Plan 2.1: Domain Model Classes

<objective>
Create all core domain model classes in `com.trafficsimulator.model` package: Vehicle (with IDM fields), Lane (with vehicle list and leader lookup), Road (multi-lane with coordinates), RoadNetwork (top-level graph), Intersection (stub for future phases), SpawnPoint and DespawnPoint (records). After this plan, all model classes compile and can be instantiated via Lombok builders.
</objective>

<tasks>

## Task 2.1.1: Create Vehicle.java

<read_first>
- /home/sebastian/traffic-simulator/.planning/phases/02-domain-model-road-network-foundation/02-RESEARCH.md (section 1.1)
</read_first>

<action>
Create `backend/src/main/java/com/trafficsimulator/model/Vehicle.java`:

```java
package com.trafficsimulator.model;

import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;

@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class Vehicle {
    private String id;
    private double position;      // metres from lane start
    private double speed;         // m/s
    private double acceleration;  // m/s² (transient, updated each tick)
    private Lane lane;            // live reference to current lane
    private double length;        // metres, default 4.5

    // IDM parameters (assigned at spawn, constant for vehicle lifetime)
    private double v0;    // desired speed m/s
    private double aMax;  // max acceleration m/s²
    private double b;     // comfortable braking deceleration m/s²
    private double s0;    // minimum gap metres
    private double T;     // desired time headway seconds

    private long spawnedAt; // tick number when created
}
```

Fields: `id` (UUID String), `position` (double, metres from lane start), `speed` (double, m/s), `acceleration` (double, m/s²), `lane` (Lane reference), `length` (double, default 4.5m), IDM params (`v0`, `aMax`, `b`, `s0`, `T`), `spawnedAt` (long tick number).
</action>

<acceptance_criteria>
- [ ] File `backend/src/main/java/com/trafficsimulator/model/Vehicle.java` exists
- [ ] `grep "private double v0;" backend/src/main/java/com/trafficsimulator/model/Vehicle.java` exits 0
- [ ] `grep "private double aMax;" backend/src/main/java/com/trafficsimulator/model/Vehicle.java` exits 0
- [ ] `grep "private double b;" backend/src/main/java/com/trafficsimulator/model/Vehicle.java` exits 0
- [ ] `grep "private double s0;" backend/src/main/java/com/trafficsimulator/model/Vehicle.java` exits 0
- [ ] `grep "private double T;" backend/src/main/java/com/trafficsimulator/model/Vehicle.java` exits 0
- [ ] `grep "@Builder" backend/src/main/java/com/trafficsimulator/model/Vehicle.java` exits 0
- [ ] `grep "private Lane lane;" backend/src/main/java/com/trafficsimulator/model/Vehicle.java` exits 0
</acceptance_criteria>

## Task 2.1.2: Create Lane.java

<read_first>
- /home/sebastian/traffic-simulator/.planning/phases/02-domain-model-road-network-foundation/02-RESEARCH.md (section 1.2)
</read_first>

<action>
Create `backend/src/main/java/com/trafficsimulator/model/Lane.java`:

```java
package com.trafficsimulator.model;

import lombok.Builder;
import lombok.Data;

import java.util.ArrayList;
import java.util.List;

@Data
@Builder
public class Lane {
    private String id;           // globally unique: "r1-lane0"
    private int laneIndex;       // 0-based within parent road
    private Road road;           // back-reference to parent road
    private double length;       // metres (copied from road)
    private double maxSpeed;     // m/s
    private boolean active;      // for Phase 7 road narrowing

    @Builder.Default
    private List<Vehicle> vehicles = new ArrayList<>();

    /**
     * Returns the vehicle directly ahead of the given vehicle in this lane,
     * or null if none exists.
     */
    public Vehicle getLeader(Vehicle vehicle) {
        Vehicle leader = null;
        for (Vehicle v : vehicles) {
            if (v != vehicle && v.getPosition() > vehicle.getPosition()) {
                if (leader == null || v.getPosition() < leader.getPosition()) {
                    leader = v;
                }
            }
        }
        return leader;
    }
}
```

Fields: `id` (String, format "r1-lane0"), `laneIndex` (int), `road` (Road back-reference), `length` (double), `maxSpeed` (double), `active` (boolean), `vehicles` (List<Vehicle>, default empty ArrayList). Method `getLeader(Vehicle)` returns the nearest vehicle ahead by position.
</action>

<acceptance_criteria>
- [ ] File `backend/src/main/java/com/trafficsimulator/model/Lane.java` exists
- [ ] `grep "private List<Vehicle> vehicles" backend/src/main/java/com/trafficsimulator/model/Lane.java` exits 0
- [ ] `grep "public Vehicle getLeader" backend/src/main/java/com/trafficsimulator/model/Lane.java` exits 0
- [ ] `grep "private Road road;" backend/src/main/java/com/trafficsimulator/model/Lane.java` exits 0
- [ ] `grep "private boolean active;" backend/src/main/java/com/trafficsimulator/model/Lane.java` exits 0
</acceptance_criteria>

## Task 2.1.3: Create Road.java

<read_first>
- /home/sebastian/traffic-simulator/.planning/phases/02-domain-model-road-network-foundation/02-RESEARCH.md (section 1.3)
</read_first>

<action>
Create `backend/src/main/java/com/trafficsimulator/model/Road.java`:

```java
package com.trafficsimulator.model;

import lombok.Builder;
import lombok.Data;

import java.util.List;

@Data
@Builder
public class Road {
    private String id;           // matches JSON config (e.g., "r1")
    private String name;         // human-readable label
    private List<Lane> lanes;    // index-ordered (lane 0 = rightmost)
    private double length;       // metres
    private double speedLimit;   // m/s
    private double startX;       // canvas/world X of road start node
    private double startY;       // canvas/world Y of road start node
    private double endX;         // canvas/world X of road end node
    private double endY;         // canvas/world Y of road end node
    private String fromNodeId;   // connected node ID
    private String toNodeId;     // connected node ID
}
```

Fields: `id`, `name`, `lanes` (List<Lane>), `length`, `speedLimit`, `startX`/`startY`/`endX`/`endY` (coordinates), `fromNodeId`/`toNodeId`.
</action>

<acceptance_criteria>
- [ ] File `backend/src/main/java/com/trafficsimulator/model/Road.java` exists
- [ ] `grep "private List<Lane> lanes;" backend/src/main/java/com/trafficsimulator/model/Road.java` exits 0
- [ ] `grep "private double speedLimit;" backend/src/main/java/com/trafficsimulator/model/Road.java` exits 0
- [ ] `grep "private double startX;" backend/src/main/java/com/trafficsimulator/model/Road.java` exits 0
- [ ] `grep "private String fromNodeId;" backend/src/main/java/com/trafficsimulator/model/Road.java` exits 0
</acceptance_criteria>

## Task 2.1.4: Create SpawnPoint.java and DespawnPoint.java records

<read_first>
- /home/sebastian/traffic-simulator/.planning/phases/02-domain-model-road-network-foundation/02-RESEARCH.md (section 1.4)
</read_first>

<action>
Create `backend/src/main/java/com/trafficsimulator/model/SpawnPoint.java`:

```java
package com.trafficsimulator.model;

public record SpawnPoint(String roadId, int laneIndex, double position) {}
```

Create `backend/src/main/java/com/trafficsimulator/model/DespawnPoint.java`:

```java
package com.trafficsimulator.model;

public record DespawnPoint(String roadId, int laneIndex, double position) {}
```

Java 17 records used for pure immutable data carriers.
</action>

<acceptance_criteria>
- [ ] File `backend/src/main/java/com/trafficsimulator/model/SpawnPoint.java` exists
- [ ] File `backend/src/main/java/com/trafficsimulator/model/DespawnPoint.java` exists
- [ ] `grep "public record SpawnPoint" backend/src/main/java/com/trafficsimulator/model/SpawnPoint.java` exits 0
- [ ] `grep "public record DespawnPoint" backend/src/main/java/com/trafficsimulator/model/DespawnPoint.java` exits 0
</acceptance_criteria>

## Task 2.1.5: Create RoadNetwork.java

<read_first>
- /home/sebastian/traffic-simulator/.planning/phases/02-domain-model-road-network-foundation/02-RESEARCH.md (section 1.4)
</read_first>

<action>
Create `backend/src/main/java/com/trafficsimulator/model/RoadNetwork.java`:

```java
package com.trafficsimulator.model;

import lombok.Builder;
import lombok.Data;

import java.util.LinkedHashMap;
import java.util.List;
import java.util.Map;

@Data
@Builder
public class RoadNetwork {
    private String id;                                // map scenario id

    @Builder.Default
    private Map<String, Road> roads = new LinkedHashMap<>();

    @Builder.Default
    private Map<String, Intersection> intersections = new LinkedHashMap<>();

    private List<SpawnPoint> spawnPoints;
    private List<DespawnPoint> despawnPoints;
}
```

Fields: `id` (String), `roads` (Map<String, Road> keyed by road ID), `intersections` (Map<String, Intersection>), `spawnPoints` (List<SpawnPoint>), `despawnPoints` (List<DespawnPoint>).
</action>

<acceptance_criteria>
- [ ] File `backend/src/main/java/com/trafficsimulator/model/RoadNetwork.java` exists
- [ ] `grep "private Map<String, Road> roads" backend/src/main/java/com/trafficsimulator/model/RoadNetwork.java` exits 0
- [ ] `grep "private Map<String, Intersection> intersections" backend/src/main/java/com/trafficsimulator/model/RoadNetwork.java` exits 0
- [ ] `grep "private List<SpawnPoint> spawnPoints" backend/src/main/java/com/trafficsimulator/model/RoadNetwork.java` exits 0
- [ ] `grep "private List<DespawnPoint> despawnPoints" backend/src/main/java/com/trafficsimulator/model/RoadNetwork.java` exits 0
</acceptance_criteria>

## Task 2.1.6: Create IntersectionType.java and Intersection.java

<read_first>
- /home/sebastian/traffic-simulator/.planning/phases/02-domain-model-road-network-foundation/02-RESEARCH.md (section 1.5)
</read_first>

<action>
Create `backend/src/main/java/com/trafficsimulator/model/IntersectionType.java`:

```java
package com.trafficsimulator.model;

public enum IntersectionType {
    SIGNAL, ROUNDABOUT, PRIORITY, NONE
}
```

Create `backend/src/main/java/com/trafficsimulator/model/Intersection.java`:

```java
package com.trafficsimulator.model;

import lombok.Builder;
import lombok.Data;

import java.util.ArrayList;
import java.util.List;

@Data
@Builder
public class Intersection {
    private String id;
    private IntersectionType type;

    @Builder.Default
    private List<String> connectedRoadIds = new ArrayList<>();

    // TrafficLight trafficLight; — null in Phase 2, added in Phase 8
}
```

`Intersection` is a structural stub for Phase 2; logic added in Phase 8.
</action>

<acceptance_criteria>
- [ ] File `backend/src/main/java/com/trafficsimulator/model/IntersectionType.java` exists
- [ ] File `backend/src/main/java/com/trafficsimulator/model/Intersection.java` exists
- [ ] `grep "SIGNAL, ROUNDABOUT, PRIORITY, NONE" backend/src/main/java/com/trafficsimulator/model/IntersectionType.java` exits 0
- [ ] `grep "private IntersectionType type;" backend/src/main/java/com/trafficsimulator/model/Intersection.java` exits 0
- [ ] `grep "private List<String> connectedRoadIds" backend/src/main/java/com/trafficsimulator/model/Intersection.java` exits 0
</acceptance_criteria>

</tasks>

<verification>
```bash
cd /home/sebastian/traffic-simulator/backend && mvn compile -q
echo "Exit code: $?"
```
Must exit 0. All 8 model classes compile with Lombok annotation processing.

```bash
grep -r "class Vehicle" backend/src/main/java/com/trafficsimulator/model/
grep -r "class Lane" backend/src/main/java/com/trafficsimulator/model/
grep -r "class Road " backend/src/main/java/com/trafficsimulator/model/
grep -r "class RoadNetwork" backend/src/main/java/com/trafficsimulator/model/
grep -r "class Intersection " backend/src/main/java/com/trafficsimulator/model/
grep -r "enum IntersectionType" backend/src/main/java/com/trafficsimulator/model/
grep -r "record SpawnPoint" backend/src/main/java/com/trafficsimulator/model/
grep -r "record DespawnPoint" backend/src/main/java/com/trafficsimulator/model/
```
All 8 greps must find exactly one match each.
</verification>

<must_haves>
- Vehicle has all 5 IDM parameters: `v0`, `aMax`, `b`, `s0`, `T` (SIM-02)
- Lane has `vehicles` list and `getLeader(Vehicle)` method
- Road supports multiple lanes via `List<Lane>` (ROAD-01)
- RoadNetwork has `spawnPoints` and `despawnPoints` lists
- All classes use Lombok `@Data` and `@Builder`
- SpawnPoint and DespawnPoint are Java 17 records
</must_haves>
