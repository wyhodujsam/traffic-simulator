---
phase: 16-combined-scenario
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/resources/maps/combined-loop.json
autonomous: true
requirements: [MAP-01]

must_haves:
  truths:
    - "Map appears in the scenario selector dropdown"
    - "Simulation starts without errors when the map is selected"
    - "Vehicles circulate on the loop road continuously"
    - "Vehicles can despawn at edge exit points"
    - "Roundabout, signalized intersection, and highway merge all function simultaneously"
  artifacts:
    - path: "backend/src/main/resources/maps/combined-loop.json"
      provides: "Combined scenario JSON map"
      contains: "combined-loop"
  key_links:
    - from: "backend/src/main/resources/maps/combined-loop.json"
      to: "MapLoader.java"
      via: "classpath:maps/*.json auto-discovery"
      pattern: "combined-loop"
---

<objective>
Create a complex multi-feature scenario JSON map that combines a roundabout, a signalized intersection, a highway merge, and a loop road. Vehicles circulate on the loop or despawn off-screen.

Purpose: Showcase all traffic features simultaneously — roundabout yield logic, signal phases, merge priority, and continuous loop circulation — in a single scenario.
Output: `backend/src/main/resources/maps/combined-loop.json` auto-discovered by the map selector.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/CONVENTIONS.md

@backend/src/main/resources/maps/roundabout.json
@backend/src/main/resources/maps/four-way-signal.json
@backend/src/main/resources/maps/highway-merge.json
@backend/src/main/java/com/trafficsimulator/config/MapConfig.java
@backend/src/main/java/com/trafficsimulator/config/MapLoader.java

<interfaces>
<!-- MapConfig schema — the JSON must conform to these types -->

MapConfig fields:
  id: String
  name: String
  description: String
  nodes: List<NodeConfig>        — {id, type: "ENTRY"|"EXIT"|"INTERSECTION", x, y}
  roads: List<RoadConfig>        — {id, name, fromNodeId, toNodeId, length, speedLimit, laneCount, closedLanes?}
  intersections: List<IntersectionConfig> — {nodeId, type: "SIGNAL"|"ROUNDABOUT"|"PRIORITY", signalPhases?, intersectionSize?, roundaboutCapacity?}
  spawnPoints: List<SpawnPointConfig>    — {roadId, laneIndex, position}
  despawnPoints: List<DespawnPointConfig> — {roadId, laneIndex, position}
  defaultSpawnRate: double

SignalPhaseConfig: {greenRoadIds: List<String>, durationMs: long, type: "GREEN"|"YELLOW"|"ALL_RED"}

Maps are auto-discovered from classpath:maps/*.json — no registration needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create combined-loop.json scenario map</name>
  <files>backend/src/main/resources/maps/combined-loop.json</files>
  <read_first>
    - backend/src/main/resources/maps/roundabout.json (roundabout pattern with ring roads)
    - backend/src/main/resources/maps/four-way-signal.json (signal phase pattern)
    - backend/src/main/resources/maps/highway-merge.json (merge with PRIORITY intersection)
    - backend/src/main/java/com/trafficsimulator/config/MapConfig.java (schema reference)
  </read_first>
  <action>
Create `backend/src/main/resources/maps/combined-loop.json` with the following topology:

**Map ID:** `combined-loop`
**Map name:** `Combined Scenario — Roundabout, Signals, Merge & Loop`
**Description:** `Complex scenario combining a roundabout, signalized intersection, highway merge, and a circulation loop. Some vehicles loop continuously while others despawn at map edges.`

**Layout (coordinate space ~1200 x 800):**

There are 3 main features connected by roads, plus a loop that brings vehicles back:

1. **ROUNDABOUT** (center-left, ~x:300 y:300):
   - 4-node ring: n_ring_n (300,272), n_ring_e (328,300), n_ring_s (300,328), n_ring_w (272,300)
   - Ring roads: n→w, w→s, s→e, e→n (length 22.0 each, speedLimit 8.3, 1 lane)
   - All 4 ring nodes: type ROUNDABOUT, intersectionSize 12, roundaboutCapacity 8

2. **SIGNALIZED INTERSECTION** (right side, ~x:800 y:300):
   - Single node: n_signal (800, 300), type SIGNAL, intersectionSize 40
   - Signal phases: NS green 20s, NS yellow 3s, all-red 2s, EW green 20s, EW yellow 3s, all-red 2s

3. **HIGHWAY MERGE** (bottom-left, feeds into roundabout):
   - Highway entry from bottom: n_hwy_entry (100, 650) type ENTRY
   - Ramp entry from bottom-right: n_ramp_entry (350, 650) type ENTRY
   - Merge point: n_merge (200, 500) type INTERSECTION, PRIORITY
   - Highway before merge: 2 lanes, length 200, speedLimit 25.0
   - Ramp: 1 lane, length 200, speedLimit 18.0
   - Highway after merge to roundabout south: 2 lanes, from n_merge to n_ring_s, length 200, speedLimit 20.0

**Connecting roads:**

- **Roundabout east exit -> Signal west approach:**
  Road from n_ring_e to n_signal, length 400, speedLimit 13.9, 2 lanes (the main arterial)

- **Signal east exit -> despawn (off-screen right):**
  n_signal to n_east_exit (1150, 300) EXIT, length 300, speedLimit 13.9, 2 lanes

- **Signal north exit -> despawn (off-screen top):**
  n_signal to n_north_exit (800, 50) EXIT, length 200, speedLimit 13.9, 1 lane

**THE LOOP (key feature):**

- **Signal south exit -> loop road -> roundabout west approach:**
  n_signal to n_loop_south (800, 550) via road length 200, speedLimit 13.9, 1 lane
  n_loop_south to n_loop_west (100, 550) via road length 600, speedLimit 16.0, 1 lane (long bottom road)
  n_loop_west to n_ring_w via road length 250, speedLimit 13.9, 1 lane (connects back to roundabout)

  Make n_loop_south and n_loop_west type INTERSECTION with type NONE (pass-through nodes for geometry).
  Actually, since NONE is not in the intersection types, just don't list them in the intersections array — nodes without intersection config are pass-through by default.

- **External approach from north into roundabout:**
  n_north_entry (300, 50) ENTRY to n_ring_n, length 200, speedLimit 11.1, 1 lane

- **Roundabout north exit -> despawn:**
  n_ring_n to n_north_desp (300, 50) EXIT, length 200, speedLimit 11.1, 1 lane

**Signal intersection inbound roads (for signal phases):**
- r_to_signal (from roundabout east) — already defined above
- r_signal_north_in: n_signal_north_entry (800, 50) ENTRY to n_signal, length 200, speedLimit 13.9, 1 lane
  NOTE: This conflicts with n_north_exit at same coords. Use different node: n_sig_north_entry (850, 50) ENTRY

Actually, let me simplify. The signal needs at least 2 opposed approaches for its phases to be meaningful. The approaches are:
- West (from roundabout): r_rndbt_to_signal
- North: r_sig_north_in from n_sig_north_entry (850, 50) ENTRY

Signal phases:
- Phase 1: GREEN for [r_rndbt_to_signal] (west approach), 20000ms
- Phase 2: YELLOW for [r_rndbt_to_signal], 3000ms
- Phase 3: ALL_RED, 2000ms
- Phase 4: GREEN for [r_sig_north_in] (north approach), 15000ms
- Phase 5: YELLOW for [r_sig_north_in], 3000ms
- Phase 6: ALL_RED, 2000ms

**Complete node list:**
- n_north_entry (300, 50) ENTRY — north approach to roundabout
- n_north_desp (300, 50) EXIT — north exit from roundabout (same visual point, separate node)
- n_ring_n (300, 272) INTERSECTION
- n_ring_e (328, 300) INTERSECTION
- n_ring_s (300, 328) INTERSECTION
- n_ring_w (272, 300) INTERSECTION
- n_hwy_entry (100, 650) ENTRY — highway main entry
- n_ramp_entry (400, 650) ENTRY — ramp entry
- n_merge (200, 500) INTERSECTION — merge point
- n_signal (800, 300) INTERSECTION — signalized intersection
- n_sig_north_entry (850, 50) ENTRY — signal north approach
- n_east_exit (1150, 300) EXIT — despawn east
- n_north_sig_exit (850, 50) EXIT — despawn north from signal (NOTE: same coords as entry, use n_sig_north_desp at (800, 50))
  Actually, avoid reusing coords. Place:
  - n_sig_north_entry at (830, 50) ENTRY
  - n_sig_north_desp at (870, 50) EXIT
- n_loop_bend (800, 550) INTERSECTION — just a geometry node (no intersection entry needed)
  Actually the simplest approach: make loop nodes regular INTERSECTION nodes but do NOT add them to the intersections array. The engine treats them as pass-through.
  Wait — nodes with type INTERSECTION that don't have an intersection config might cause issues. Let me check...

Let me use a simpler, cleaner layout. Use these exact nodes and roads:

**NODES (16 total):**
```
n_north_entry    ENTRY         300   50    — spawn: north approach to roundabout
n_north_exit     EXIT          300   50    — despawn: north from roundabout
n_ring_n         INTERSECTION  300  272    — roundabout north
n_ring_e         INTERSECTION  328  300    — roundabout east
n_ring_s         INTERSECTION  300  328    — roundabout south
n_ring_w         INTERSECTION  272  300    — roundabout west
n_hwy_entry      ENTRY         100  650    — spawn: highway main
n_ramp_entry     ENTRY         400  650    — spawn: highway ramp
n_merge          INTERSECTION  250  480    — merge point (PRIORITY)
n_signal         INTERSECTION  800  300    — signalized intersection
n_sig_n_entry    ENTRY         830   50    — spawn: signal north approach
n_sig_n_exit     EXIT          770   50    — despawn: signal north exit
n_east_exit      EXIT         1150  300    — despawn: east exit
n_loop_se        INTERSECTION  800  550    — loop bend SE (pass-through)
n_loop_sw        INTERSECTION  100  550    — loop bend SW (pass-through)
```

Wait, that's 15 nodes. Let me not overthink. INTERSECTION-type nodes that don't have an intersection config entry might be handled as NONE/pass-through. Let me check the MapLoader to be sure.

Actually, to be safe, give n_loop_se and n_loop_sw intersection entries with type "PRIORITY" and no signalPhases. That's the safest pattern — PRIORITY intersections just use right-of-way rules, and with only one inbound road, vehicles just pass through.

**ROADS (17 total):**
```
r_north_in       n_north_entry -> n_ring_n     L:200  SL:11.1  lanes:1
r_north_out      n_ring_n -> n_north_exit      L:200  SL:11.1  lanes:1
r_ring_nw        n_ring_n -> n_ring_w          L:22   SL:8.3   lanes:1
r_ring_ws        n_ring_w -> n_ring_s          L:22   SL:8.3   lanes:1
r_ring_se        n_ring_s -> n_ring_e          L:22   SL:8.3   lanes:1
r_ring_en        n_ring_e -> n_ring_n          L:22   SL:8.3   lanes:1
r_hwy_before     n_hwy_entry -> n_merge        L:200  SL:25.0  lanes:2
r_ramp           n_ramp_entry -> n_merge       L:220  SL:18.0  lanes:1
r_hwy_to_rndbt   n_merge -> n_ring_s           L:180  SL:16.0  lanes:2
r_rndbt_to_sig   n_ring_e -> n_signal          L:400  SL:13.9  lanes:2
r_sig_n_in       n_sig_n_entry -> n_signal     L:200  SL:13.9  lanes:1
r_sig_n_out      n_signal -> n_sig_n_exit      L:200  SL:13.9  lanes:1
r_east_out       n_signal -> n_east_exit       L:300  SL:13.9  lanes:2
r_sig_to_loop    n_signal -> n_loop_se         L:200  SL:13.9  lanes:1
r_loop_bottom    n_loop_se -> n_loop_sw        L:600  SL:16.0  lanes:1
r_loop_to_rndbt  n_loop_sw -> n_ring_w         L:250  SL:13.9  lanes:1
```

Wait, that's only 16 roads. Let me count: r_north_in, r_north_out, r_ring_nw, r_ring_ws, r_ring_se, r_ring_en, r_hwy_before, r_ramp, r_hwy_to_rndbt, r_rndbt_to_sig, r_sig_n_in, r_sig_n_out, r_east_out, r_sig_to_loop, r_loop_bottom, r_loop_to_rndbt = 16 roads. Good.

**INTERSECTIONS (8 total):**
```
n_ring_n   ROUNDABOUT  intersectionSize:12  roundaboutCapacity:8
n_ring_e   ROUNDABOUT  intersectionSize:12  roundaboutCapacity:8
n_ring_s   ROUNDABOUT  intersectionSize:12  roundaboutCapacity:8
n_ring_w   ROUNDABOUT  intersectionSize:12  roundaboutCapacity:8
n_merge    PRIORITY
n_signal   SIGNAL      intersectionSize:40  with 6 signal phases
n_loop_se  PRIORITY
n_loop_sw  PRIORITY
```

**SIGNAL PHASES for n_signal:**
```
{ greenRoadIds: ["r_rndbt_to_sig"],  durationMs: 20000, type: "GREEN" }
{ greenRoadIds: ["r_rndbt_to_sig"],  durationMs: 3000,  type: "YELLOW" }
{ greenRoadIds: [],                  durationMs: 2000,  type: "ALL_RED" }
{ greenRoadIds: ["r_sig_n_in"],      durationMs: 15000, type: "GREEN" }
{ greenRoadIds: ["r_sig_n_in"],      durationMs: 3000,  type: "YELLOW" }
{ greenRoadIds: [],                  durationMs: 2000,  type: "ALL_RED" }
```

**SPAWN POINTS (5):**
```
r_north_in   lane:0  pos:0.0
r_hwy_before lane:0  pos:0.0
r_hwy_before lane:1  pos:0.0
r_ramp       lane:0  pos:0.0
r_sig_n_in   lane:0  pos:0.0
```

**DESPAWN POINTS (5):**
```
r_north_out  lane:0  pos:200.0
r_sig_n_out  lane:0  pos:200.0
r_east_out   lane:0  pos:300.0
r_east_out   lane:1  pos:300.0
```

Note: There is NO despawn on the loop roads — vehicles that go south from the signal travel the loop back to the roundabout and circulate. Vehicles that go east or north from the signal despawn. This creates the "some loop, some despawn" behavior.

**defaultSpawnRate:** 0.8 (moderate — enough to see all features without overload)

**THE LOOP PATH:** Vehicles spawned at highway or north entries -> roundabout -> east to signal -> (some go east=despawn, some go south=loop) -> loop bottom road -> back to roundabout west -> circulate again. The routing is probabilistic based on the engine's existing routing logic at intersections.

Write the complete JSON file with proper formatting. Use the exact field names and structure from MapConfig.java. Follow the patterns in roundabout.json and four-way-signal.json exactly.

IMPORTANT: Verify all node IDs referenced in roads match actual node definitions. Verify all road IDs referenced in signalPhases/spawnPoints/despawnPoints match actual road definitions. Double-check fromNodeId/toNodeId consistency.
  </action>
  <verify>
    <automated>cd /home/sebastian/traffic-simulator && python3 -c "
import json, sys
with open('backend/src/main/resources/maps/combined-loop.json') as f:
    m = json.load(f)
node_ids = {n['id'] for n in m['nodes']}
road_ids = {r['id'] for r in m['roads']}
errors = []
for r in m['roads']:
    if r['fromNodeId'] not in node_ids: errors.append(f'Road {r[\"id\"]} fromNodeId {r[\"fromNodeId\"]} missing')
    if r['toNodeId'] not in node_ids: errors.append(f'Road {r[\"id\"]} toNodeId {r[\"toNodeId\"]} missing')
for i in m['intersections']:
    if i['nodeId'] not in node_ids: errors.append(f'Intersection nodeId {i[\"nodeId\"]} missing')
for sp in m['spawnPoints']:
    if sp['roadId'] not in road_ids: errors.append(f'SpawnPoint roadId {sp[\"roadId\"]} missing')
for dp in m['despawnPoints']:
    if dp['roadId'] not in road_ids: errors.append(f'DespawnPoint roadId {dp[\"roadId\"]} missing')
# Check loop exists: signal -> loop_se -> loop_sw -> ring_w path
loop_roads = [r for r in m['roads'] if r['id'] in ('r_sig_to_loop','r_loop_bottom','r_loop_to_rndbt')]
assert len(loop_roads) == 3, f'Expected 3 loop roads, got {len(loop_roads)}'
# Check all 3 feature types present
types = {i['type'] for i in m['intersections']}
assert 'ROUNDABOUT' in types, 'Missing ROUNDABOUT'
assert 'SIGNAL' in types, 'Missing SIGNAL'
assert 'PRIORITY' in types, 'Missing PRIORITY (merge)'
if errors: print('ERRORS:', errors); sys.exit(1)
print('JSON validation PASSED: all refs valid, loop present, all 3 intersection types found')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists at backend/src/main/resources/maps/combined-loop.json
    - JSON is valid and parseable
    - All node references in roads resolve to defined nodes
    - All road references in spawnPoints/despawnPoints/signalPhases resolve to defined roads
    - Contains ROUNDABOUT, SIGNAL, and PRIORITY intersection types
    - Loop path exists: signal south -> loop SE -> loop SW -> roundabout west
    - Despawn points exist at east exit and north exits
    - No despawn on loop roads (vehicles circulate)
  </acceptance_criteria>
  <done>combined-loop.json exists, passes JSON reference validation, contains all 3 intersection types plus a circulation loop</done>
</task>

<task type="auto">
  <name>Task 2: Smoke-test map loading via backend compilation and REST endpoint</name>
  <files>backend/src/main/resources/maps/combined-loop.json</files>
  <read_first>
    - backend/src/main/resources/maps/combined-loop.json (the file just created)
    - backend/src/main/java/com/trafficsimulator/config/MapLoader.java (loading logic)
    - backend/src/main/java/com/trafficsimulator/controller/SimulationController.java (REST /api/maps endpoint)
  </read_first>
  <action>
Run the backend test suite to verify combined-loop.json loads without errors. The existing MapLoaderTest and integration tests exercise map loading. If all tests pass, the new map is structurally valid.

Then start the backend briefly and hit GET /api/maps to confirm `combined-loop` appears in the list. Use curl with a timeout.

Steps:
1. Run `cd /home/sebastian/traffic-simulator/backend && mvn test -q` — all tests must pass (no MapLoader/validation failures from the new JSON)
2. If tests fail with a map validation error pointing to combined-loop.json, fix the JSON (likely a node reference issue or missing field)
3. Start backend: `cd /home/sebastian/traffic-simulator/backend && mvn spring-boot:run -q &` then `sleep 5 && curl -s http://localhost:8080/api/maps | python3 -m json.tool` to verify combined-loop appears
4. Kill the backend process after verification

If the backend port is already in use or the startup takes too long, rely solely on the test suite passing as proof of correctness.
  </action>
  <verify>
    <automated>cd /home/sebastian/traffic-simulator/backend && mvn test -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `mvn test` passes with 0 failures and 0 errors
    - No MapLoader or MapValidator exceptions referencing combined-loop
    - Backend test suite exit code is 0
  </acceptance_criteria>
  <done>All backend tests pass including map loading validation; combined-loop.json is a valid scenario the engine can load</done>
</task>

</tasks>

<verification>
1. JSON reference integrity: all node/road IDs are consistent (Task 1 automated check)
2. Backend compilation and tests pass (Task 2)
3. Map contains all 3 intersection types: ROUNDABOUT, SIGNAL, PRIORITY
4. Loop topology exists: vehicles can circulate signal -> SE bend -> SW bend -> roundabout -> signal
5. Despawn points exist for vehicles leaving the map (east, north)
</verification>

<success_criteria>
- combined-loop.json exists in backend/src/main/resources/maps/
- All backend tests pass (mvn test exit 0)
- Map has roundabout (4 ring nodes), signalized intersection (6-phase cycle), highway merge (PRIORITY), and loop roads
- Some vehicles circulate (loop path has no despawn), some despawn (east/north exits)
</success_criteria>

<output>
After completion, create `.planning/phases/16-combined-scenario-roundabout-signal-merge-loop/16-01-SUMMARY.md`
</output>
