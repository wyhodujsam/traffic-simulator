---
phase: 24
plan: 07
type: execute
wave: 6
depends_on: [24-05, 24-06]
files_modified:
  - backend/docs/osm-converters.md
autonomous: false
requirements: []
tags: [docs, close-gate, verification]

must_haves:
  truths:
    - "backend/docs/osm-converters.md documents the three-converter architecture (no '(future)' placeholder left for Phase 24)"
    - "Docs record: pinned osm2streets git SHA, build date, binary size, SHA-256, rebuild recipe"
    - "Docs document the optional lanes[] RoadConfig field contract and MVP lane types"
    - "Docs document the known roundabout-detection gap (osm2streets IntersectionControl has no Roundabout variant)"
    - "Full backend suite passes: ./mvnw test green (0 failures, 0 errors)"
    - "Full frontend suite passes: npm run test green (Vitest, 0 failures)"
    - "Full Playwright suite passes: npx playwright test green (8 specs, 0 failures)"
    - "Phase 18 + Phase 23 endpoints respond byte-identical to pre-Phase-24 snapshots (no 'lanes' key)"
  artifacts:
    - path: "backend/docs/osm-converters.md"
      provides: "Three-way OSM converter architecture documentation"
      contains: "osm2streets"
  key_links:
    - from: "backend/docs/osm-converters.md"
      to: "backend/bin/README.md"
      via: "cross-reference for binary provenance"
      pattern: "backend/bin/README.md"
---

<objective>
Update `backend/docs/osm-converters.md` to replace the "(future)" Phase 24 row with a full section documenting the osm2streets integration, the `lanes[]` optional field contract, the roundabout-detection gap, and the binary provenance. Run the full-suite close gate (backend + vitest + Playwright) and capture the test-count deltas.

Purpose: Documentation + close-gate are the last responsibilities — the phase is incomplete until external-facing docs match reality and the full suite is green. No new code lands here; this plan is the phase's acceptance criteria made literal.

Output: Updated osm-converters.md, recorded test counts, phase SUMMARY traceability entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-osm2streets-integration-use-a-b-street-s-osm2streets-rust-wasm-for-lane-level-street-network-with-markings-turn-lanes-intersection-shapes-new-api-osm-fetch-roads-o2s-endpoint-to-a-b-compare-against-phase-18-and-phase-23/24-RESEARCH.md
@.planning/phases/24-osm2streets-integration-use-a-b-street-s-osm2streets-rust-wasm-for-lane-level-street-network-with-markings-turn-lanes-intersection-shapes-new-api-osm-fetch-roads-o2s-endpoint-to-a-b-compare-against-phase-18-and-phase-23/24-01-SUMMARY.md
@.planning/phases/24-osm2streets-integration-use-a-b-street-s-osm2streets-rust-wasm-for-lane-level-street-network-with-markings-turn-lanes-intersection-shapes-new-api-osm-fetch-roads-o2s-endpoint-to-a-b-compare-against-phase-18-and-phase-23/24-04-SUMMARY.md
@backend/docs/osm-converters.md
@backend/bin/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update osm-converters.md with Phase 24 section + lanes[] contract + roundabout gap</name>
  <read_first>
    - backend/docs/osm-converters.md (FULL — find the "(future) osm2streets" row + "Adding a third converter (Phase 24 recipe)" section)
    - backend/bin/README.md (from Plan 24-01 — binary provenance)
    - .planning/phases/24-.../24-01-SUMMARY.md — osm2streets git SHA pin, build date, binary size
    - .planning/phases/24-.../24-04-SUMMARY.md — roundabout-gap evidence
    - .planning/phases/24-.../24-RESEARCH.md §What "Done" Looks Like
  </read_first>
  <files>
    backend/docs/osm-converters.md
  </files>
  <action>
    **Step 1 — Update the top converter table.** Replace the current Phase 24 row:
    ```
    | (future) osm2streets | 24    | POST /api/osm/fetch-roads-o2s   | TBD | TBD |
    ```
    with:
    ```
    | osm2streets (Rust)   | 24    | `POST /api/osm/fetch-roads-o2s` | `Osm2StreetsService`         | `"osm2streets"`                       |
    ```

    **Step 2 — Update the "Non-goals" paragraph.** Remove the sentence "does NOT ship lane-level geometry (turn lanes, crosswalks, intersection polygons — that is Phase 24's scope)" and replace with: "Phase 24 DOES ship lane-level geometry via an optional `lanes[]` field on RoadConfig (see §Optional lanes[] field below). Canvas rendering of turn-lane markings and intersection polygons remains out of scope."

    **Step 3 — Add a new "## Phase 24 data flow" section** AFTER "## Phase 23 data flow", showing the subprocess pipeline:
    ```
    BboxRequest
       │
       ▼
    OverpassXmlFetcher.fetchXmlBytes (shared helper, extracted in Plan 24-04)
       │   Overpass XML bytes
       ▼
    Osm2StreetsService.executeCli (ProcessBuilder → backend/bin/osm2streets-cli-linux-x64)
       │   stdin = XML, stdout = StreetNetwork JSON, 30s timeout
       ▼
    StreetNetworkMapper.map (JSON → MapConfig, package-private static)
       │   - uses OsmConversionUtils for projection, lane clamp, signals, assembly
       │   - IDs prefixed "o2s-"
       │   - optional lanes[] populated from lane_specs_ltr
       ▼
    MapValidator.validate (hard gate)
       │
       ▼
    MapConfig → 200 OK
    ```

    **Step 4 — Add a new "## Optional lanes[] field (Phase 24)" section** documenting the contract:
    - `MapConfig.RoadConfig.lanes` is nullable `List<LaneConfig>`
    - `@JsonInclude(JsonInclude.Include.NON_NULL)` annotation means Phase 18/23 responses are byte-identical (no "lanes":null stanza)
    - MVP lane types: `driving`, `parking`, `cycling`, `sidewalk` (others dropped)
    - Field names: `type` (String), `width` (double, metres), `direction` ("forward" | "backward" | "both")
    - MapValidator does NOT validate the field — simulation engine IGNORES it in this phase
    - Future phases may wire lane-aware IDM/MOBIL or canvas rendering of lane markings

    **Step 5 — Add a new "## Known gap: roundabout detection" section** documenting the Pitfall 5 limitation:
    - osm2streets' `IntersectionControl` enum has no Roundabout variant (only Uncontrolled / Signed / Signalled / Construction)
    - Phase 24 classifies all non-signalled intersections as PRIORITY
    - On the roundabout.osm fixture, Phase 18/23 produce ROUNDABOUT intersections while Phase 24 produces PRIORITY
    - This is accepted for MVP; future phases may add WGS84 cross-reference to the retained Overpass XML to recover the junction=roundabout tag

    **Step 6 — Add binary provenance cross-reference.** Append a "## Binary provenance" section:
    - Link to `backend/bin/README.md` for build recipe
    - Record the pinned git SHA (from 24-01-SUMMARY.md)
    - Record the binary SHA-256 (from backend/bin/osm2streets-cli-linux-x64.sha256)
    - Document refresh cadence: "manual only; re-pin SHA, rebuild, swap binary, update hash"

    **Step 7 — Update the "Adding a third converter (Phase 24 recipe)" section heading** to "## Adding a fourth converter (future recipe)" and update example code to reference the now-three-converter baseline. Keep the steps generic.
  </action>
  <verify>
    <automated>grep -q "osm2streets" backend/docs/osm-converters.md && ! grep -q "(future) osm2streets" backend/docs/osm-converters.md && grep -q "lanes\[\]" backend/docs/osm-converters.md && grep -q -i "roundabout" backend/docs/osm-converters.md && grep -q "Osm2StreetsService" backend/docs/osm-converters.md && grep -q "Phase 24 data flow\|Phase 24" backend/docs/osm-converters.md</automated>
  </verify>
  <acceptance_criteria>
    - No "(future)" marker for Phase 24 remains in the doc
    - Converter table shows 3 rows (Overpass / GraphHopper / osm2streets) with complete endpoint + service + label columns
    - New "Phase 24 data flow" section present with ASCII diagram
    - New "Optional lanes[] field (Phase 24)" section documents the contract (nullable, MVP types, Jackson behaviour)
    - New "Known gap: roundabout detection" section documents Pitfall 5
    - Binary provenance section cross-references backend/bin/README.md
    - Old "Phase 24 recipe" section renamed to "fourth converter (future)"
  </acceptance_criteria>
  <done>
    Documentation matches shipped reality; no stale "(future)" markers remain.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase close gate — full suite green + Phase 18/23 byte-identity</name>
  <read_first>
    - .planning/phases/24-.../24-RESEARCH.md §What "Done" Looks Like (7-point checklist)
    - .planning/STATE.md §Phase 22.1 completion note (baseline: 347 backend + 56 vitest + 7 playwright)
  </read_first>
  <what-built>
    Seven plans shipped: Rust wrapper + binary (24-01), MapConfig schema (24-02), service scaffold + executeCli (24-03), mapper + end-to-end service (24-04), controller endpoint + comparison (24-05), frontend button + Playwright (24-06), docs (24-07).
  </what-built>
  <how-to-verify>
    **Run these commands IN ORDER on the development host and record outputs in SUMMARY.md:**

    1. **Backend full suite:**
       ```bash
       cd backend && ./mvnw test 2>&1 | tee /tmp/backend-test.log
       grep -E "Tests run:" /tmp/backend-test.log | tail -1
       ```
       Expected: "Tests run: >=380, Failures: 0, Errors: 0, Skipped: <=2".
       Record exact numbers.

    2. **Frontend unit tests (Vitest):**
       ```bash
       cd frontend && npm run test -- --run 2>&1 | tee /tmp/frontend-test.log
       ```
       Expected: 56+ tests, 0 failures.

    3. **Frontend TypeScript:**
       ```bash
       cd frontend && npx tsc --noEmit
       ```
       Expected: zero output (no errors).

    4. **Playwright full suite:**
       ```bash
       cd frontend && npx playwright test 2>&1 | tee /tmp/playwright.log
       tail -20 /tmp/playwright.log
       ```
       Expected: "8 passed" (7 pre-existing + 1 new).

    5. **Phase 18 byte-identity regression (OFFLINE — against live backend):**
       Start the backend (`cd backend && ./mvnw spring-boot:run &`), then:
       ```bash
       curl -s -X POST http://localhost:8086/api/osm/fetch-roads \
         -H "Content-Type: application/json" \
         -d '{"south":52.229,"west":21.011,"north":52.231,"east":21.014}' \
         | python3 -m json.tool | grep -c '"lanes"'
       ```
       Expected: `0` — Phase 18 response MUST NOT contain "lanes" key.

    6. **Phase 23 byte-identity regression:**
       ```bash
       curl -s -X POST http://localhost:8086/api/osm/fetch-roads-gh \
         -H "Content-Type: application/json" \
         -d '{"south":52.229,"west":21.011,"north":52.231,"east":21.014}' \
         | python3 -m json.tool | grep -c '"lanes"'
       ```
       Expected: `0`.

    7. **Phase 24 smoke — lanes[] DOES appear:**
       ```bash
       curl -s -X POST http://localhost:8086/api/osm/fetch-roads-o2s \
         -H "Content-Type: application/json" \
         -d '{"south":52.229,"west":21.011,"north":52.231,"east":21.014}' \
         | python3 -m json.tool | grep -c '"lanes"'
       ```
       Expected: `>=1` (lanes[] present on at least one road). If the binary path or Overpass connectivity fails, this may return `0`; in that case, fall back to the stubbed Playwright fixture as the byte-check (step 4 already covers it).

    8. **Binary executable + checksum:**
       ```bash
       sha256sum -c backend/bin/osm2streets-cli-linux-x64.sha256
       file backend/bin/osm2streets-cli-linux-x64
       ```
       Expected: `OK`; `ELF 64-bit` + `statically linked`.

    **Record all captured numbers + outputs in `.planning/phases/24-.../24-07-SUMMARY.md`.**
  </how-to-verify>
  <resume-signal>
    Type "approved" to mark Phase 24 closed, OR describe specific issues (which test failed, which assertion broke). If a regression is found in Phase 18/23, open a gap plan via `/gsd-plan-phase 24 --gaps`.
  </resume-signal>
</task>

</tasks>

<verification>
  - osm-converters.md reflects shipped state (no "(future)" for Phase 24)
  - Backend suite ≥ 380 tests green
  - Frontend vitest + TypeScript clean
  - Playwright 8 specs green
  - Phase 18/23 endpoints byte-identical (no "lanes" key) — verified via curl
  - osm2streets endpoint surfaces lanes[] when the binary is runnable
  - Binary SHA-256 verified, `file` reports statically linked ELF
</verification>

<success_criteria>
  - All 7 "What Done Looks Like" RESEARCH §13 items satisfied
  - No regression in Phase 18/23 behaviour or test counts
  - Documentation + tests + binary + SHA all committed
  - Phase STATE.md updated by the close gate (Phase 24 marked Complete)
</success_criteria>

<output>
After completion, create `.planning/phases/24-osm2streets-integration-.../24-07-SUMMARY.md` with:
- Final test counts (backend / vitest / playwright)
- Byte-identity proof (curl output snippets for Phase 18 + Phase 23)
- osm2streets smoke evidence (at least one lanes[] entry in response)
- Binary SHA-256 + file command output
- Link to the updated osm-converters.md section

Also: update `.planning/STATE.md`:
- Move Phase 24 status to ✓ Complete
- Append a Session Log entry with the close date + test counts
- Increment `completed_phases` counter
</output>
