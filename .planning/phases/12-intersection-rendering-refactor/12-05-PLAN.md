---
phase: 12
plan: 5
name: "Visual verification and tuning"
wave: 3
depends_on: ["12-03", "12-04"]
requirements: []
files_modified:
  - "backend/src/main/resources/maps/four-way-signal.json"
  - "frontend/src/rendering/drawIntersections.ts"
  - "frontend/src/rendering/drawRoads.ts"
  - "frontend/src/rendering/drawTrafficLights.ts"
autonomous: false
---

# Plan 12.5: Visual verification and tuning

End-to-end visual verification of the intersection rendering. Load the four-way-signal map and check that roads, intersections, and traffic lights render correctly without visual artifacts. Tune `intersectionSize` and rendering if needed.

## Task 12.5.1: Verify intersection rendering end-to-end

<read_first>
- /home/sebastian/traffic-simulator/backend/src/main/resources/maps/four-way-signal.json
- /home/sebastian/traffic-simulator/frontend/src/rendering/drawRoads.ts
- /home/sebastian/traffic-simulator/frontend/src/rendering/drawIntersections.ts
- /home/sebastian/traffic-simulator/frontend/src/rendering/drawTrafficLights.ts
- /home/sebastian/traffic-simulator/frontend/src/components/SimulationCanvas.tsx
</read_first>

<action>
1. Start the backend and frontend.
2. Load the `four-way-signal` map.
3. Visually verify:
   - Roads end cleanly at the intersection boundary (no overlapping markings at center)
   - The intersection box fills the gap between road ends seamlessly
   - No triangular convergence or visual chaos at the center point
   - Traffic lights still render correctly on top of/near the intersection
   - Yellow center lines still appear on paired approach roads outside the intersection
   - Lane dividers stop at the intersection boundary
   - Road boundaries (white edge lines) stop at the intersection boundary
   - Vehicles still render and animate correctly through the intersection
   - Obstacle placement still works on roads outside the intersection zone

4. Tune `intersectionSize` in `four-way-signal.json` if needed:
   - Too small: road markings still overlap inside the box
   - Too large: visible gap between road ends and intersection box edges
   - Target: box exactly covers the area where all 8 road ends converge
   - Given 1-lane roads at LANE_WIDTH_PX=14, and in/out pairs offset by ~14px, the box needs to cover at least 2*14 + some margin = ~40px. Start with 40, adjust if needed.

5. If traffic lights are positioned at road endpoints (which are now inside the intersection box), they may need position adjustment. Check if `TrafficLightDto.x/y` uses the original road endpoint (n_center at 400,300) — if so, the lights still render at the intersection center, which is fine since they are on the dynamic layer drawn on top.

6. Fix any visual issues found during verification.
</action>

<acceptance_criteria>
- [ ] Four-way intersection renders as a clean dark gray box with roads approaching from 4 directions
- [ ] No overlapping white lines, dashed lines, or yellow lines inside the intersection zone
- [ ] Traffic lights visible and correctly colored at intersection approaches
- [ ] Vehicles animate smoothly through the intersection (no visual glitches at clip boundaries)
- [ ] Obstacle click-to-place still works on all 8 roads
- [ ] No console errors or TypeScript compilation errors
- [ ] `cd backend && mvn compile -q` exits 0
- [ ] `cd frontend && npx tsc --noEmit` exits 0
</acceptance_criteria>
