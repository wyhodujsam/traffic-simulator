---
phase: 23-graphhopper-based-osm-parser
plan: 06
type: execute
wave: 6
depends_on:
  - "23-05"
files_modified:
  - frontend/e2e/osm-bbox-gh.spec.ts
autonomous: true
requirements:
  - GH-06

must_haves:
  truths:
    - "New Playwright spec `osm-bbox-gh.spec.ts` exists in `frontend/e2e/` directory"
    - "Spec stubs POST /api/osm/fetch-roads-gh, clicks the new button, and asserts the sidebar transitions to `result` state with GraphHopper-prefixed headline"
    - "Spec reuses the existing `osm-fetch-roads-response.json` fixture (no new fixture file needed)"
    - "Playwright suite total goes from 6 specs (Phase 22.1 baseline) to 7 specs, all green"
    - "Existing `osm-bbox.spec.ts` (Phase 18 flow) still green — regression gate"
  artifacts:
    - path: "frontend/e2e/osm-bbox-gh.spec.ts"
      provides: "Playwright smoke test for the GraphHopper OSM flow with stubbed backend"
      contains: "/api/osm/fetch-roads-gh"
      min_lines: 40
  key_links:
    - from: "frontend/e2e/osm-bbox-gh.spec.ts"
      to: "frontend/e2e/fixtures/osm-fetch-roads-response.json"
      via: "readFileSync + JSON.parse (same pattern as osm-bbox.spec.ts)"
      pattern: "osm-fetch-roads-response\\.json"
    - from: "frontend/e2e/osm-bbox-gh.spec.ts"
      to: "frontend/src/components/MapSidebar.tsx"
      via: "getByRole('button', { name: /Fetch roads \\(GraphHopper\\)/i })"
      pattern: "Fetch roads \\(GraphHopper\\)"
---

<objective>
Add a Playwright smoke test `osm-bbox-gh.spec.ts` that mirrors Phase 22.1's `osm-bbox.spec.ts` for the new GraphHopper flow. Validates the end-to-end wiring: new button rendered → click fires fetch → stubbed response → sidebar transitions to result state with the GraphHopper origin label.

Per 23-CONTEXT.md "Testing": "Frontend Playwright spec `osm-bbox-gh.spec.ts` mirroring `osm-bbox.spec.ts` — stubbed `/api/osm/fetch-roads-gh` with canned MapConfig, assert sidebar result. Reuse the fixture pattern from 22.1."

**Scope (tight):**
- ONE new `.spec.ts` file.
- NO new fixture — reuse `osm-fetch-roads-response.json` (same canned MapConfig shape from Phase 22.1).
- NO backend dependency — route is stubbed via `page.route`.

**Out of scope:**
- Modifying existing specs.
- Adding fixtures.
- CI config changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-graphhopper-based-osm-parser-swap-custom-overpass-converter-for-graphhopper-osmreader-waysegmentparser-to-get-cleaner-intersection-splitting-coexists-with-existing-osmpipelineservice-via-a-api-osm-fetch-roads-gh-endpoint-for-a-b-comparison/23-CONTEXT.md
@.planning/phases/23-graphhopper-based-osm-parser-swap-custom-overpass-converter-for-graphhopper-osmreader-waysegmentparser-to-get-cleaner-intersection-splitting-coexists-with-existing-osmpipelineservice-via-a-api-osm-fetch-roads-gh-endpoint-for-a-b-comparison/23-05-SUMMARY.md
@frontend/e2e/osm-bbox.spec.ts
@frontend/e2e/fixtures/osm-fetch-roads-response.json
@frontend/src/components/MapSidebar.tsx
@frontend/src/pages/MapPage.tsx

<interfaces>
<!-- Existing Phase 22.1 osm-bbox.spec.ts pattern — we clone this structurally -->
```typescript
import { test, expect } from '@playwright/test';
import { readFileSync } from 'node:fs';
import { fileURLToPath } from 'node:url';

const fixturePath = fileURLToPath(new URL('./fixtures/osm-fetch-roads-response.json', import.meta.url));
const cannedMapConfig = JSON.parse(readFileSync(fixturePath, 'utf-8'));

test.describe('OSM bbox fetch flow', () => {
  test('stubbed fetch: click Fetch Roads -> sidebar shows 3 roads, 2 intersections', async ({ page }) => {
    await page.route('**/api/osm/fetch-roads', async (route) => {
      expect(route.request().method()).toBe('POST');
      // ... asserts bbox body shape ...
      await route.fulfill({ status: 200, contentType: 'application/json', body: JSON.stringify(cannedMapConfig) });
    });
    await page.goto('/map');
    await expect(page.getByText(/^\d+m x \d+m$/)).toBeVisible({ timeout: 10_000 });
    await page.getByRole('button', { name: /^Fetch Roads$/ }).click();
    await expect(page.getByText('3 roads, 2 intersections')).toBeVisible({ timeout: 10_000 });
    await expect(page.getByRole('button', { name: /^Run Simulation$/ })).toBeVisible();
    await expect(page.getByRole('button', { name: /^Export JSON$/ })).toBeVisible();
    await expect(page.getByRole('button', { name: /^New Selection$/ })).toBeVisible();
  });
});
```

<!-- The fixture (canned MapConfig) reused without modification -->
<!-- frontend/e2e/fixtures/osm-fetch-roads-response.json shape: -->
```json
{ "id": "test-bbox", "name": "OSM Import", "nodes": [...], "roads": [3 items], "intersections": [2 items], ... }
```

<!-- The button we're clicking (from Plan 05 MapSidebar) -->
<!-- Accessible name: "Fetch roads (GraphHopper)" (lowercase 'r' in roads, parens around GraphHopper) -->
```
<button>Fetch roads (GraphHopper)</button>
```

<!-- The result heading (from Plan 05 MapSidebar) -->
<!-- First <p>: "GraphHopper" (the origin label) -->
<!-- Second <p>: "3 roads, 2 intersections" (the counts) -->
```
<p>GraphHopper</p>
<p>3 roads, 2 intersections</p>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author osm-bbox-gh.spec.ts mirroring the Phase 22.1 OSM spec</name>
  <read_first>
    - frontend/e2e/osm-bbox.spec.ts (all 54 lines — the exact structural template)
    - frontend/e2e/fixtures/osm-fetch-roads-response.json (the fixture we're reusing)
    - frontend/src/components/MapSidebar.tsx (confirm the button accessible name and result-state rendering from Plan 05)
  </read_first>
  <files>frontend/e2e/osm-bbox-gh.spec.ts</files>
  <behavior>
    - Test navigates to /map, waits for Leaflet to stabilise (bbox meters text appears).
    - Stubs the GraphHopper endpoint with the canned fixture.
    - Clicks the new button by its accessible name.
    - Asserts:
      1. Sidebar transitions to result state (indicator: "Run Simulation" button visible).
      2. Origin heading `GraphHopper` visible.
      3. Counts line `3 roads, 2 intersections` visible.
      4. Result-state action buttons (Run Simulation, Export JSON, New Selection) all visible.
    - Does NOT click Run Simulation — that path is covered elsewhere and requires a real STOMP backend.
  </behavior>
  <action>
    Create `frontend/e2e/osm-bbox-gh.spec.ts` with this content:

    ```typescript
    import { test, expect } from '@playwright/test';
    import { readFileSync } from 'node:fs';
    import { fileURLToPath } from 'node:url';

    /**
     * Phase 23 smoke test: OSM bbox fetch flow via the GraphHopper converter.
     *
     * Mirrors osm-bbox.spec.ts (Phase 22.1) structurally:
     *  - Same fixture (canned MapConfig with 3 roads + 2 intersections)
     *  - Stubbed /api/osm/fetch-roads-gh endpoint — no real backend call
     *  - Clicks "Fetch roads (GraphHopper)" button
     *  - Asserts sidebar shows `GraphHopper` origin heading + counts
     *
     * Does NOT exercise Run Simulation. No STOMP side effects.
     */

    const fixturePath = fileURLToPath(
      new URL('./fixtures/osm-fetch-roads-response.json', import.meta.url)
    );
    const cannedMapConfig = JSON.parse(readFileSync(fixturePath, 'utf-8'));

    test.describe('OSM bbox fetch flow — GraphHopper', () => {
      test('stubbed fetch: click Fetch roads (GraphHopper) -> sidebar shows GraphHopper heading + 3 roads, 2 intersections', async ({ page }) => {
        // 1. Stub ONLY the GraphHopper endpoint. Do NOT stub the Phase 18 endpoint
        //    (keeps regression guard from osm-bbox.spec.ts intact — the other spec still
        //    controls its own stub).
        await page.route('**/api/osm/fetch-roads-gh', async (route) => {
          expect(route.request().method()).toBe('POST');
          const body = route.request().postDataJSON() as {
            south: number; west: number; north: number; east: number;
          };
          expect(body).toHaveProperty('south');
          expect(body).toHaveProperty('west');
          expect(body).toHaveProperty('north');
          expect(body).toHaveProperty('east');
          await route.fulfill({
            status: 200,
            contentType: 'application/json',
            body: JSON.stringify(cannedMapConfig),
          });
        });

        // 2. Navigate to /map.
        await page.goto('/map');

        // 3. Wait for Leaflet to emit the first bbox (sidebar meters text appears).
        await expect(page.getByText(/^\d+m x \d+m$/)).toBeVisible({ timeout: 10_000 });

        // 4. Click the new Fetch roads (GraphHopper) button.
        await page.getByRole('button', { name: /^Fetch roads \(GraphHopper\)$/ }).click();

        // 5. Assert sidebar transitions to result — origin heading visible.
        await expect(page.getByText('GraphHopper', { exact: true })).toBeVisible({ timeout: 10_000 });

        // 6. Assert counts line renders — matches Phase 22.1 assertion shape.
        await expect(page.getByText('3 roads, 2 intersections')).toBeVisible({ timeout: 10_000 });

        // 7. Assert result-state action buttons appear.
        await expect(page.getByRole('button', { name: /^Run Simulation$/ })).toBeVisible();
        await expect(page.getByRole('button', { name: /^Export JSON$/ })).toBeVisible();
        await expect(page.getByRole('button', { name: /^New Selection$/ })).toBeVisible();
      });
    });
    ```

    **Important details:**
    - Use `{ exact: true }` on `getByText('GraphHopper', ...)` to avoid matching the button label `"Fetch roads (GraphHopper)"` — we want the result-state heading `<p>GraphHopper</p>` specifically.
    - Use the regex `/^Fetch roads \(GraphHopper\)$/` with anchors to avoid accidentally matching a broader button text.
    - Do NOT add an `afterEach` cleanup — the test never sends STOMP commands so the backend simulation state is not dirtied. This matches Phase 22.1 `osm-bbox.spec.ts`'s rationale (see the file's comment block).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx playwright test osm-bbox-gh.spec.ts --reporter=line 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/e2e/osm-bbox-gh.spec.ts` exists.
    - `grep -c "test(" frontend/e2e/osm-bbox-gh.spec.ts` returns at least `1`.
    - `grep "page.route.*/api/osm/fetch-roads-gh" frontend/e2e/osm-bbox-gh.spec.ts` returns a match.
    - `grep "Fetch roads (GraphHopper)" frontend/e2e/osm-bbox-gh.spec.ts` returns at least 1 match.
    - `grep "3 roads, 2 intersections" frontend/e2e/osm-bbox-gh.spec.ts` returns a match.
    - `grep "osm-fetch-roads-response.json" frontend/e2e/osm-bbox-gh.spec.ts` returns a match (fixture reuse).
    - `cd frontend && npx playwright test osm-bbox-gh.spec.ts --reporter=line` exits 0 with `1 passed`.
    - `cd frontend && npx playwright test osm-bbox.spec.ts --reporter=line` ALSO exits 0 with `1 passed` (Phase 22.1 regression gate).
    - `cd frontend && npx playwright test --reporter=line` full suite exits 0 with ≥ 7 passing specs (6 baseline + 1 new).
  </acceptance_criteria>
  <done>osm-bbox-gh.spec.ts passes in isolation and inside the full Playwright suite; Phase 22.1 osm-bbox.spec.ts remains green.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx playwright test --reporter=line` exits 0.
- Total Playwright spec count: 7 (was 6).
- `cd frontend && npx playwright test osm-bbox.spec.ts osm-bbox-gh.spec.ts --reporter=line` passes both in the same run (confirms the two specs can coexist without route-stub conflicts).
</verification>

<success_criteria>
1. New spec file exists, exercises the stubbed GraphHopper flow, asserts the origin heading + count line + result-state buttons.
2. Reuses the Phase 22.1 fixture — no new JSON fixture file.
3. All 7 Playwright specs (6 baseline + 1 new) pass.
4. The test intentionally does NOT click Run Simulation — no backend state pollution, same discipline as Phase 22.1's osm-bbox spec.
</success_criteria>

<output>
After completion, create `.planning/phases/23-graphhopper-based-osm-parser-swap-custom-overpass-converter-for-graphhopper-osmreader-waysegmentparser-to-get-cleaner-intersection-splitting-coexists-with-existing-osmpipelineservice-via-a-api-osm-fetch-roads-gh-endpoint-for-a-b-comparison/23-06-SUMMARY.md` covering: spec file path, Playwright command + exit code, total spec count before/after.
</output>
