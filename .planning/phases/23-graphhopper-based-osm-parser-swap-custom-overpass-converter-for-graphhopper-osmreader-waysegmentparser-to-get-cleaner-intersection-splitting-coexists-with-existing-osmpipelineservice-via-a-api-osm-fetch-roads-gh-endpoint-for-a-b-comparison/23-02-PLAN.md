---
phase: 23-graphhopper-based-osm-parser
plan: 02
type: execute
wave: 2
depends_on:
  - "23-01"
files_modified:
  - backend/src/main/java/com/trafficsimulator/service/OsmConverter.java
  - backend/src/main/java/com/trafficsimulator/service/OsmPipelineService.java
autonomous: true
requirements:
  - GH-IFACE-01

must_haves:
  truths:
    - "`OsmConverter` interface exists with a single contract method `MapConfig fetchAndConvert(BboxRequest)` + `default String converterName()`"
    - "`OsmPipelineService implements OsmConverter` — Phase 18 now participates in the converter abstraction"
    - "Phase 18 public behaviour unchanged — existing callers (OsmController, tests) work without edits"
    - "The interface is ready for Phase 24 osm2streets (a third implementation later)"
    - "Full backend suite green (no regressions)"
  artifacts:
    - path: "backend/src/main/java/com/trafficsimulator/service/OsmConverter.java"
      provides: "The shared converter contract + default converterName() for A/B logging"
      contains: "interface OsmConverter"
      min_lines: 15
  key_links:
    - from: "backend/src/main/java/com/trafficsimulator/service/OsmPipelineService.java"
      to: "backend/src/main/java/com/trafficsimulator/service/OsmConverter.java"
      via: "`implements OsmConverter`"
      pattern: "implements OsmConverter"
---

<objective>
Introduce the thin `OsmConverter` interface so Phase 23's `GraphHopperOsmService` (Plan 03) and Phase 24's osm2streets service can plug into the same contract. Retrofit `OsmPipelineService` to implement it — this is a single-line addition, no logic change.

Per 23-CONTEXT.md "Claude's Discretion" (bullet about `OsmConverter`): lean toward YES. Per 23-RESEARCH.md §11 and "Open Questions" #3: include a default `converterName()` method for A/B logging.

**Scope (narrow):**
- Create `OsmConverter` interface with TWO methods: `fetchAndConvert(BboxRequest)` (abstract) + `converterName()` (default, returns simple class name).
- Add `implements OsmConverter` to `OsmPipelineService` declaration line. Signature of `fetchAndConvert` already matches — no body change.
- Do NOT wire anything into `OsmController` yet — that's Plan 04.
- Do NOT create `GraphHopperOsmService` yet — that's Plan 03.

**Wave 0 gate (from 23-SPIKE.md):** before starting this plan, read `23-SPIKE.md`'s `## Decision for Wave 1+` section. If A7 verdict says context-aborts (bad), then the interface must add a `default boolean isAvailable() { return true; }` method and the Plan 04 controller design must gate by it. If A7 says context-survives (good), proceed with interface as-specified below.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/23-graphhopper-based-osm-parser-swap-custom-overpass-converter-for-graphhopper-osmreader-waysegmentparser-to-get-cleaner-intersection-splitting-coexists-with-existing-osmpipelineservice-via-a-api-osm-fetch-roads-gh-endpoint-for-a-b-comparison/23-CONTEXT.md
@.planning/phases/23-graphhopper-based-osm-parser-swap-custom-overpass-converter-for-graphhopper-osmreader-waysegmentparser-to-get-cleaner-intersection-splitting-coexists-with-existing-osmpipelineservice-via-a-api-osm-fetch-roads-gh-endpoint-for-a-b-comparison/23-RESEARCH.md
@.planning/phases/23-graphhopper-based-osm-parser-swap-custom-overpass-converter-for-graphhopper-osmreader-waysegmentparser-to-get-cleaner-intersection-splitting-coexists-with-existing-osmpipelineservice-via-a-api-osm-fetch-roads-gh-endpoint-for-a-b-comparison/23-00-SUMMARY.md
@.planning/phases/23-graphhopper-based-osm-parser-swap-custom-overpass-converter-for-graphhopper-osmreader-waysegmentparser-to-get-cleaner-intersection-splitting-coexists-with-existing-osmpipelineservice-via-a-api-osm-fetch-roads-gh-endpoint-for-a-b-comparison/23-SPIKE.md
@backend/src/main/java/com/trafficsimulator/service/OsmPipelineService.java
@backend/src/main/java/com/trafficsimulator/config/MapConfig.java
@backend/src/main/java/com/trafficsimulator/dto/BboxRequest.java

<interfaces>
<!-- Existing OsmPipelineService public method — the contract we're abstracting. -->
```java
// backend/src/main/java/com/trafficsimulator/service/OsmPipelineService.java line 99:
public MapConfig fetchAndConvert(BboxRequest bbox);
```

<!-- The interface we are creating: -->
```java
package com.trafficsimulator.service;

import com.trafficsimulator.config.MapConfig;
import com.trafficsimulator.dto.BboxRequest;

public interface OsmConverter {
    /**
     * Fetch OSM data for the given bbox and convert it to a simulation-ready MapConfig.
     * Implementations MUST produce output that passes MapValidator.
     *
     * @throws IllegalStateException if no usable roads are found in the area
     * @throws org.springframework.web.client.RestClientException if the upstream OSM source is unavailable
     */
    MapConfig fetchAndConvert(BboxRequest bbox);

    /**
     * Human-readable identifier for A/B logging. Defaults to simple class name.
     * Override for custom labelling (e.g. "Overpass", "GraphHopper", "osm2streets").
     */
    default String converterName() {
        return getClass().getSimpleName();
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OsmConverter interface and retrofit OsmPipelineService</name>
  <read_first>
    - backend/src/main/java/com/trafficsimulator/service/OsmPipelineService.java (line 29–32 class declaration, line 99–106 fetchAndConvert signature)
    - .planning/phases/23-.../23-RESEARCH.md §11 "Extract" subsection (lines ~903–914) — the interface shape
    - .planning/phases/23-.../23-SPIKE.md (A7 verdict — determines whether we need `isAvailable()` guard)
    - backend/src/test/java/com/trafficsimulator/architecture/ArchitectureTest.java (first 50 lines — layered architecture rules that OsmConverter must satisfy)
  </read_first>
  <files>backend/src/main/java/com/trafficsimulator/service/OsmConverter.java, backend/src/main/java/com/trafficsimulator/service/OsmPipelineService.java</files>
  <action>
    **Step 1:** Create `backend/src/main/java/com/trafficsimulator/service/OsmConverter.java` with the interface defined in the `<interfaces>` block above. Include full Javadoc for both methods. The interface is `public interface OsmConverter { ... }` — no annotations needed. Same package as implementations (service).

    **Step 2 (conditional — read 23-SPIKE.md first):**
    - If `23-SPIKE.md` `## A7` section says **Result: FAIL** (context survives — desirable): interface is complete as specified. Skip the rest of this step.
    - If `23-SPIKE.md` `## A7` section says **Result: PASS** (context aborts — undesirable): additionally add a `default boolean isAvailable() { return true; }` method to the interface. Plan 04 will use this to gate the GraphHopper endpoint. Document this addition in the summary.

    **Step 3:** Edit `backend/src/main/java/com/trafficsimulator/service/OsmPipelineService.java`:

    Change line 32 from:
    ```java
    public class OsmPipelineService {
    ```
    to:
    ```java
    public class OsmPipelineService implements OsmConverter {
    ```

    Add `@Override` on the `fetchAndConvert` method (line 99):
    ```java
    @Override
    public MapConfig fetchAndConvert(BboxRequest bbox) {
    ```

    Override `converterName()` to return a stable human-readable label (per RESEARCH.md §11 "Extract"):
    ```java
    @Override
    public String converterName() {
        return "Overpass";
    }
    ```

    Add the method immediately after `fetchAndConvert` (before the private `fetchFromMirrors`). Naming "Overpass" (NOT "OsmPipelineService") so A/B logs read cleanly: `"Overpass: 42 roads / 11 intersections; GraphHopper: 38 roads / 9 intersections"`.

    **DO NOT** change any existing test. `OsmControllerTest` injects `OsmPipelineService` by concrete type — that still works (`OsmPipelineService` is the concrete class, it just happens to also satisfy `OsmConverter`).

    **Architecture note:** `OsmConverter` lives in `..service..` package (same as implementations). The existing `ArchitectureTest.layered_architecture` rule does NOT define a `Service` layer explicitly — service package is considered "below Controller". Adding an interface in this package does not introduce any layer violation. Confirm by running ArchitectureTest.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; ./mvnw -q test -Dtest=ArchitectureTest,OsmPipelineServiceTest,OsmControllerTest</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/src/main/java/com/trafficsimulator/service/OsmConverter.java` exists.
    - `grep -c "interface OsmConverter" backend/src/main/java/com/trafficsimulator/service/OsmConverter.java` returns `1`.
    - `grep -c "MapConfig fetchAndConvert(BboxRequest" backend/src/main/java/com/trafficsimulator/service/OsmConverter.java` returns `1`.
    - `grep -c "default String converterName" backend/src/main/java/com/trafficsimulator/service/OsmConverter.java` returns `1`.
    - `grep "implements OsmConverter" backend/src/main/java/com/trafficsimulator/service/OsmPipelineService.java` returns a match.
    - `grep -cE "@Override" backend/src/main/java/com/trafficsimulator/service/OsmPipelineService.java` returns at least `2` (fetchAndConvert + converterName).
    - `grep 'return "Overpass"' backend/src/main/java/com/trafficsimulator/service/OsmPipelineService.java` returns a match.
    - `./mvnw -pl backend test -Dtest=ArchitectureTest` exits 0 — layered architecture still valid.
    - `./mvnw -pl backend test -Dtest=OsmPipelineServiceTest,OsmControllerTest` exits 0 — no regression.
    - `./mvnw -pl backend test` full suite exits 0.
    - If 23-SPIKE.md `## A7` says **Result: PASS** (context aborts), additionally `grep "default boolean isAvailable" backend/src/main/java/com/trafficsimulator/service/OsmConverter.java` returns a match. Otherwise (A7 FAIL / context survives), that grep returns 0 matches — `isAvailable` is intentionally absent.
  </acceptance_criteria>
  <done>OsmConverter interface exists with two (or three, per A7 verdict) methods; OsmPipelineService implements it and labels itself "Overpass"; all 308+ tests green.</done>
</task>

</tasks>

<verification>
- `./mvnw -pl backend test` full suite passes.
- `OsmConverter.java` is a pure interface (no `class` keyword, no `@Component`, no `@Service`).
- `OsmPipelineService` is the only current implementation (`grep -r "implements OsmConverter" backend/src/main/java/`).
- `OsmPipelineService.converterName()` returns the literal "Overpass".
</verification>

<success_criteria>
1. `OsmConverter` interface ships with abstract `fetchAndConvert(BboxRequest) -> MapConfig` and default `converterName() -> String`.
2. `OsmPipelineService` implements `OsmConverter` and overrides `converterName()` to return "Overpass".
3. Full backend suite green. No behaviour change on `/api/osm/fetch-roads`.
4. The interface is ready for Plan 03 (`GraphHopperOsmService implements OsmConverter`) and future Phase 24 (`Osm2StreetsService implements OsmConverter`).
</success_criteria>

<output>
After completion, create `.planning/phases/23-graphhopper-based-osm-parser-swap-custom-overpass-converter-for-graphhopper-osmreader-waysegmentparser-to-get-cleaner-intersection-splitting-coexists-with-existing-osmpipelineservice-via-a-api-osm-fetch-roads-gh-endpoint-for-a-b-comparison/23-02-SUMMARY.md` covering: interface shape, whether `isAvailable()` was added (A7-dependent), OsmPipelineService label, test count.
</output>
