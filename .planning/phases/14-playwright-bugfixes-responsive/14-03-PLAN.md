---
phase: 14-playwright-bugfixes-responsive
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/index.html
  - frontend/src/components/ControlsPanel.tsx
autonomous: true
requirements: [BUG-5, BUG-6]

must_haves:
  truths:
    - "Browser tab shows 'Traffic Simulator' as page title"
    - "Start/Pause/Stop buttons fit in a single row without wrapping"
  artifacts:
    - path: "frontend/index.html"
      provides: "Correct page title"
      contains: "Traffic Simulator"
    - path: "frontend/src/components/ControlsPanel.tsx"
      provides: "Compact button layout"
      contains: "flexWrap"
  key_links: []
---

<objective>
Fix BUG-5 (generic page title) and BUG-6 (buttons wrapping to two lines).

Purpose: Two trivial cosmetic issues — wrong page title and button layout overflow.

Output: Correct title tag and compact button styling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@bugs.md

<interfaces>
From frontend/index.html:
```html
<!-- Line 6: Current title -->
<title>Vite + React + TS</title>
```

From frontend/src/components/ControlsPanel.tsx:
```tsx
// Line 80-91: buttonStyle function
const buttonStyle = (enabled: boolean): React.CSSProperties => ({
  padding: '8px 16px',    // <-- padding too large, causes wrapping
  margin: '4px',
  // ...
  fontSize: '14px',
});

// Line 154-175: Button container — plain div with no flex layout
<div style={{ marginBottom: '16px' }}>
  <button ...>Start/Resume</button>
  <button ...>Pause</button>
  <button ...>Stop</button>
</div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix page title and button layout</name>
  <files>frontend/index.html, frontend/src/components/ControlsPanel.tsx</files>
  <read_first>
    - frontend/index.html
    - frontend/src/components/ControlsPanel.tsx
  </read_first>
  <action>
    **BUG-5 — Page title (per D-BUG-5):**
    In `frontend/index.html`, change line 6:
    - FROM: `<title>Vite + React + TS</title>`
    - TO: `<title>Traffic Simulator</title>`

    **BUG-6 — Button layout (per D-BUG-6):**
    In `frontend/src/components/ControlsPanel.tsx`:

    1. Change the button container div (around line 154) to use flex layout:
       ```tsx
       <div style={{ display: 'flex', gap: '4px', marginBottom: '16px', flexWrap: 'nowrap' }}>
       ```

    2. Update `buttonStyle` (line 80-91) to reduce padding and remove margin (gap handles spacing):
       - Change `padding: '8px 16px'` to `padding: '6px 10px'`
       - Change `margin: '4px'` to `margin: '0'` (flex gap replaces margin)
       - Change `fontSize: '14px'` to `fontSize: '13px'`
       - Add `flex: '1'` so buttons share available width equally
       - Add `whiteSpace: 'nowrap'` to prevent text wrapping inside buttons
  </action>
  <verify>
    <automated>cd /home/sebastian/traffic-simulator/frontend && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/index.html` contains `<title>Traffic Simulator</title>`
    - Button container has `display: 'flex'` and `flexWrap: 'nowrap'`
    - `buttonStyle` has `padding: '6px 10px'` and `margin: '0'`
    - `buttonStyle` has `whiteSpace: 'nowrap'`
    - `tsc --noEmit` passes with zero errors
  </acceptance_criteria>
  <done>
    Browser tab shows "Traffic Simulator" as page title.
    Start/Pause/Stop buttons render in a single row without wrapping at 1280px viewport.
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` — zero errors
- `grep -n 'Traffic Simulator' frontend/index.html` — shows title tag
- `grep -n 'flexWrap' frontend/src/components/ControlsPanel.tsx` — shows nowrap on button container
</verification>

<success_criteria>
- Page title reads "Traffic Simulator" in browser tab
- Three control buttons fit in one row at 1280x720 and narrower (down to sidebar width)
</success_criteria>

<output>
After completion, create `.planning/phases/14-playwright-bugfixes-responsive/14-03-SUMMARY.md`
</output>
