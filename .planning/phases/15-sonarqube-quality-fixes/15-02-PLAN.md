---
phase: 15-sonarqube-quality-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/trafficsimulator/scheduler/SnapshotBuilder.java
  - backend/src/main/java/com/trafficsimulator/engine/PhysicsEngine.java
  - backend/src/main/java/com/trafficsimulator/scheduler/TickEmitter.java
  - backend/src/main/java/com/trafficsimulator/controller/SimulationController.java
autonomous: true
requirements: [SQ-03, SQ-06]

must_haves:
  truths:
    - "SnapshotBuilder.buildSnapshot() cognitive complexity is <= 15"
    - "PhysicsEngine.tick() cognitive complexity is <= 15"
    - "TickEmitter.emitTick() cognitive complexity is <= 15"
    - "SimulationController getIntersections()/postCommand() cognitive complexity is <= 15"
  artifacts:
    - path: "backend/src/main/java/com/trafficsimulator/scheduler/SnapshotBuilder.java"
      provides: "Refactored snapshot builder with extracted collection methods"
      contains: "collectVehiclesAndObstacles"
    - path: "backend/src/main/java/com/trafficsimulator/engine/PhysicsEngine.java"
      provides: "Refactored physics with extracted leader-finding logic"
      contains: "findEffectiveLeader"
    - path: "backend/src/main/java/com/trafficsimulator/scheduler/TickEmitter.java"
      provides: "Refactored tick emitter with extracted simulation pipeline"
      contains: "runSimulationPipeline"
    - path: "backend/src/main/java/com/trafficsimulator/controller/SimulationController.java"
      provides: "Refactored controller with extracted intersection geometry logic"
      contains: "computeIntersectionCenter"
  key_links:
    - from: "SnapshotBuilder.buildSnapshot()"
      to: "private helper methods"
      via: "method delegation"
      pattern: "collect(VehiclesAndObstacles|TrafficLights)|computeStats"
    - from: "PhysicsEngine.tick()"
      to: "findEffectiveLeader()"
      via: "method extraction"
      pattern: "findEffectiveLeader"
---

<objective>
Reduce cognitive complexity in SnapshotBuilder.buildSnapshot() (77-><=15), PhysicsEngine.tick() (37-><=15), TickEmitter.emitTick() (20-><=15), and SimulationController (16-><=15).

Purpose: Resolve 4 CRITICAL SonarQube violations (java:S3776) across scheduler/engine/controller files.
Output: 4 modified Java files passing all 140 tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-sonarqube-quality-fixes/15-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor SnapshotBuilder.buildSnapshot() — extract collection methods (SQ-03)</name>
  <files>backend/src/main/java/com/trafficsimulator/scheduler/SnapshotBuilder.java</files>
  <read_first>backend/src/main/java/com/trafficsimulator/scheduler/SnapshotBuilder.java</read_first>
  <action>
    Refactor `buildSnapshot()` (complexity 77) by extracting 3 private methods. The main method becomes a simple orchestrator.

    **Create a helper record to carry vehicle/obstacle collection results:**
    ```java
    private record VehicleObstacleCollection(
        List<VehicleDto> vehicles, List<ObstacleDto> obstacles,
        double totalSpeed, int vehicleCount, double totalRoadLength) {}
    ```

    **Extract method 1: `collectVehiclesAndObstacles(RoadNetwork network)`**
    - Move lines 31-51 (the road/lane iteration collecting VehicleDtos, ObstacleDtos, totalSpeed, vehicleCount, totalRoadLength)
    - Returns `VehicleObstacleCollection` record
    - Guard: if network is null, return empty collection with zeros

    **Extract method 2: `collectTrafficLights(RoadNetwork network)`**
    - Move lines 54-93 (intersection iteration, traffic light state, box-blocking check)
    - Returns `List<TrafficLightDto>`
    - Guard: if network is null, return empty list

    **Extract sub-method within collectTrafficLights: `isBoxBlocked(Intersection ixtn, String inRoadId, RoadNetwork network)`**
    - Move lines 66-80 (the box-blocking detection loop checking outbound road space)
    - Returns `boolean`
    - This is critical for reducing nesting depth — the triple-nested for loop (outbound roads -> lanes -> vehicle check) was the main complexity driver

    **Extract method 3: `computeStats(VehicleObstacleCollection collection, IVehicleSpawner spawner)`**
    - Move lines 96-106 (avg speed, density, throughput calculation)
    - Returns `StatsDto`

    **Resulting buildSnapshot():**
    ```java
    public SimulationStateDto buildSnapshot(RoadNetwork network, long tick,
            String status, double spawnRate, double speedMultiplier,
            IVehicleSpawner vehicleSpawner, String mapId, String error) {
        VehicleObstacleCollection collection = collectVehiclesAndObstacles(network);
        List<TrafficLightDto> trafficLights = collectTrafficLights(network);
        StatsDto stats = computeStats(collection, vehicleSpawner);
        return SimulationStateDto.builder()
            .tick(tick).timestamp(System.currentTimeMillis()).status(status)
            .vehicles(collection.vehicles()).obstacles(collection.obstacles())
            .trafficLights(trafficLights).stats(stats)
            .mapId(mapId).error(error)
            .build();
    }
    ```

    CRITICAL: Do not change behavior. Same DTO fields, same values, same null handling.
  </action>
  <verify>
    <automated>cd /home/sebastian/traffic-simulator/backend && mvn test -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SnapshotBuilder.java contains methods: collectVehiclesAndObstacles, collectTrafficLights, isBoxBlocked, computeStats
    - The public buildSnapshot() method body is <=15 lines
    - All 140 tests pass
  </acceptance_criteria>
  <done>SnapshotBuilder.buildSnapshot() refactored from complexity 77 to <=15 via method extraction. SQ-03 resolved.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor PhysicsEngine.tick(), TickEmitter.emitTick(), SimulationController (SQ-06)</name>
  <files>
    backend/src/main/java/com/trafficsimulator/engine/PhysicsEngine.java
    backend/src/main/java/com/trafficsimulator/scheduler/TickEmitter.java
    backend/src/main/java/com/trafficsimulator/controller/SimulationController.java
  </files>
  <read_first>
    backend/src/main/java/com/trafficsimulator/engine/PhysicsEngine.java
    backend/src/main/java/com/trafficsimulator/scheduler/TickEmitter.java
    backend/src/main/java/com/trafficsimulator/controller/SimulationController.java
  </read_first>
  <action>
    **PhysicsEngine.tick() — complexity 37 -> <=15:**

    The main complexity comes from the leader selection logic (lines 73-110) with nested if/else chains for vehicle+obstacle+stopLine combinations.

    Extract method: `findEffectiveLeader(Vehicle vehicle, Vehicle vehicleLeader, List<Obstacle> obstacles, double stopLinePosition)`
    - Returns a new record: `private record LeaderInfo(double position, double speed, double length, boolean present) {}`
    - Move the entire leader selection logic (lines 68-110) into this method
    - The triple if/else (vehicleLeader+obstacle, vehicleLeader only, obstacle only, neither) plus stopLine override goes here
    - This reduces tick() to: iterate vehicles, call findEffectiveLeader, compute acceleration, apply guards, integrate

    Also extract: `findNearestObstacleAhead(List<Obstacle> obstacles, double vehiclePosition)`
    - Move lines 59-66 (obstacle search loop)
    - Returns `Obstacle` or null

    Also extract: `applyGuardsAndIntegrate(Vehicle vehicle, double acceleration, double dt, double laneMaxSpeed)`
    - Move lines 114-134 (NaN guard, Euler integration, speed clamping)
    - Calls `vehicle.updatePhysics(newPosition, newSpeed, acceleration)`

    **TickEmitter.emitTick() — complexity 20 -> <=15:**

    Extract: `runSimulationPipeline(RoadNetwork network, long tick)`
    - Move lines 58-93 (the entire `if (network != null)` block with sub-stepping, traffic lights, spawn, physics, lane changes, intersection transfers, despawn)
    - Called from emitTick() only when status == RUNNING and network != null

    This makes emitTick() a simple: lock -> drain -> check status -> runPipeline or get tick -> build snapshot -> broadcast -> unlock

    **SimulationController — complexity 16 -> <=15:**

    The `getIntersections()` method (lines 124-164) has the complexity. Extract:
    - `computeIntersectionCenter(Intersection ixtn, RoadNetwork network)` — lines 130-150 (road endpoint averaging loop). Returns a simple record `private record Point(double x, double y) {}`.
    - `computeIntersectionSize(Intersection ixtn, RoadNetwork network)` — lines 152-155 (max lane count calculation). Returns double.

    This reduces getIntersections() to: iterate intersections, call computeCenter, call computeSize, build DTO.

    CRITICAL: No behavior changes in any file. Same physics, same tick pipeline order, same DTOs.
  </action>
  <verify>
    <automated>cd /home/sebastian/traffic-simulator/backend && mvn test -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - PhysicsEngine.java contains methods: findEffectiveLeader, findNearestObstacleAhead, applyGuardsAndIntegrate
    - PhysicsEngine.tick() method body is <=25 lines
    - TickEmitter.java contains method: runSimulationPipeline
    - TickEmitter.emitTick() method body is <=25 lines
    - SimulationController.java contains methods: computeIntersectionCenter, computeIntersectionSize
    - All 140 tests pass
  </acceptance_criteria>
  <done>PhysicsEngine.tick(), TickEmitter.emitTick(), and SimulationController complexity all reduced to <=15. SQ-06 resolved.</done>
</task>

</tasks>

<verification>
- `cd /home/sebastian/traffic-simulator/backend && mvn test -q` — all 140 tests pass
- `grep -c "private.*collect\|private.*compute\|private.*isBox" backend/src/main/java/com/trafficsimulator/scheduler/SnapshotBuilder.java` — at least 4
- `grep -c "findEffectiveLeader\|findNearestObstacle\|applyGuards" backend/src/main/java/com/trafficsimulator/engine/PhysicsEngine.java` — at least 3
- `grep "runSimulationPipeline" backend/src/main/java/com/trafficsimulator/scheduler/TickEmitter.java` — present
- `grep "computeIntersectionCenter\|computeIntersectionSize" backend/src/main/java/com/trafficsimulator/controller/SimulationController.java` — present
</verification>

<success_criteria>
- SQ-03: SnapshotBuilder.buildSnapshot() complexity reduced from 77 to <=15
- SQ-06: PhysicsEngine.tick() from 37 to <=15, TickEmitter.emitTick() from 20 to <=15, SimulationController from 16 to <=15
- All 140 backend tests pass unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/15-sonarqube-quality-fixes/15-02-SUMMARY.md`
</output>
