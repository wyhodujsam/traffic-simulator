---
phase: 15-sonarqube-quality-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/test/java/com/trafficsimulator/engine/CommandQueueTest.java
  - backend/src/test/java/com/trafficsimulator/engine/VehicleSpawnerTest.java
  - backend/src/main/java/com/trafficsimulator/config/MapValidator.java
autonomous: true
requirements: [SQ-01, SQ-02, SQ-07]

must_haves:
  truths:
    - "CommandQueueTest line 33 test has a meaningful assertion (not assertTrue(true))"
    - "VehicleSpawnerTest line 99 test has a meaningful assertion"
    - "MapValidator.validate() cognitive complexity is <= 15"
    - "MapValidator has no duplicated string literals for 'Road ' and 'Intersection '"
  artifacts:
    - path: "backend/src/test/java/com/trafficsimulator/engine/CommandQueueTest.java"
      provides: "Test with assertion for concurrent enqueue"
      contains: "assert"
    - path: "backend/src/test/java/com/trafficsimulator/engine/VehicleSpawnerTest.java"
      provides: "Test with assertion for no-spawn-points case"
      contains: "assert"
    - path: "backend/src/main/java/com/trafficsimulator/config/MapValidator.java"
      provides: "Refactored validator with extracted methods and constants"
      contains: "ROAD_PREFIX"
  key_links:
    - from: "MapValidator.validate()"
      to: "private helper methods"
      via: "method delegation"
      pattern: "validate(Nodes|Roads|Intersections|SpawnPoints)"
---

<objective>
Fix SonarQube BLOCKER test assertion violations (SQ-01) and refactor MapValidator to reduce cognitive complexity from 103 to <=15 (SQ-02) while extracting duplicated string literals (SQ-07).

Purpose: Resolve 2 BLOCKER and 2 CRITICAL SonarQube violations across 3 files.
Output: 3 modified Java files passing all 140 tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-sonarqube-quality-fixes/15-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add meaningful assertions to CommandQueueTest and VehicleSpawnerTest (SQ-01)</name>
  <files>
    backend/src/test/java/com/trafficsimulator/engine/CommandQueueTest.java
    backend/src/test/java/com/trafficsimulator/engine/VehicleSpawnerTest.java
  </files>
  <read_first>
    backend/src/test/java/com/trafficsimulator/engine/CommandQueueTest.java
    backend/src/test/java/com/trafficsimulator/engine/VehicleSpawnerTest.java
  </read_first>
  <action>
    **CommandQueueTest.java** — `concurrentEnqueue_100Threads_noConcurrentModificationException()` at line ~33:
    - After `engine.drainCommands()` on line 54, the test has no assertion (just a comment "If no exception...").
    - Add: capture the drain result or call `engine.drainCommands()` with a counter. Since `drainCommands()` returns void, use reflection or engine state to verify. Simplest approach: after drain, verify the engine's command queue is empty by adding `assertThat(engine.getCommandQueue().isEmpty()).isTrue()` or equivalent. If `getCommandQueue()` is not accessible, add an `assertDoesNotThrow` wrapping the drain call, or count commands via a spy.
    - Preferred approach: Add `assertThat(latch.await(5, TimeUnit.SECONDS)).isTrue()` to assert all threads completed, PLUS assert that drain completes without exception by wrapping in `assertDoesNotThrow(() -> engine.drainCommands())`.

    **VehicleSpawnerTest.java** — `tick_noSpawnPoints_doesNothing()` at line ~99:
    - After `spawner.tick(1.0, emptyNetwork, 1)` on line 108, there's only a comment "No exception, no vehicles".
    - Add: `assertThat(emptyNetwork.getRoads()).isEmpty()` to verify the network is unchanged, confirming no side effects occurred. Since roads is an empty map, this verifies spawner didn't create anything.
    - Also add: verify spawner throughput is still 0: `assertThat(spawner.getThroughput()).isEqualTo(0.0)`.

    Per SQ-01: Both tests must have at least one assertion (java:S2699). Do NOT use `assertTrue(true)` — assertions must verify actual behavior.
  </action>
  <verify>
    <automated>cd /home/sebastian/traffic-simulator/backend && mvn test -pl . -Dtest="CommandQueueTest,VehicleSpawnerTest" -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - CommandQueueTest.concurrentEnqueue_100Threads has at least one assertThat/assertEquals call
    - VehicleSpawnerTest.tick_noSpawnPoints_doesNothing has at least one assertThat/assertEquals call
    - Both tests still pass
  </acceptance_criteria>
  <done>Both test methods contain meaningful assertions verifying actual behavior. SQ-01 resolved.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor MapValidator — extract methods and string constants (SQ-02, SQ-07)</name>
  <files>backend/src/main/java/com/trafficsimulator/config/MapValidator.java</files>
  <read_first>backend/src/main/java/com/trafficsimulator/config/MapValidator.java</read_first>
  <action>
    **SQ-07 — Extract duplicated string literals:**
    Add at class level:
    ```java
    private static final String ROAD_PREFIX = "Road ";
    private static final String INTERSECTION_PREFIX = "Intersection ";
    ```
    Replace all 5 occurrences of `"Road " + road.getId()` with `ROAD_PREFIX + road.getId()` and all 3 occurrences of `"Intersection " + ic.getNodeId()` with `INTERSECTION_PREFIX + ic.getNodeId()`.

    **SQ-02 — Reduce cognitive complexity from 103 to <=15:**
    Extract the monolithic `validate()` method into 5 private methods. The main `validate()` becomes a simple dispatcher:

    ```java
    public List<String> validate(MapConfig config) {
        List<String> errors = new ArrayList<>();
        validateBasicFields(config, errors);
        Set<String> nodeIds = validateNodes(config, errors);
        Set<String> roadIds = validateRoads(config, errors, nodeIds);
        validateSpawnAndDespawnPoints(config, errors, roadIds);
        validateIntersections(config, errors, roadIds);
        logResult(config, errors);
        return errors;
    }
    ```

    Extract these private methods:
    1. `validateBasicFields(MapConfig config, List<String> errors)` — lines 18-26: null/blank checks for id, nodes, roads
    2. `validateNodes(MapConfig config, List<String> errors)` — lines 29-35: unique node ID check. Returns `Set<String> nodeIds` (or empty set if nodes null).
    3. `validateRoads(MapConfig config, List<String> errors, Set<String> nodeIds)` — lines 38-56: road node refs, laneCount 1-4, positive length/speedLimit. Returns `Set<String> roadIds`. Use `ROAD_PREFIX` constant. Guard: if config.getNodes()==null || config.getRoads()==null, return empty set.
    4. `validateSpawnAndDespawnPoints(MapConfig config, List<String> errors, Set<String> roadIds)` — lines 60-76: spawn/despawn road references. Guard: if roadIds empty, return.
    5. `validateIntersections(MapConfig config, List<String> errors, Set<String> roadIds)` — lines 79-118: orphan check, SIGNAL phase validation. Guard: if intersections or roads null, return. Use `INTERSECTION_PREFIX` constant.
    6. `logResult(MapConfig config, List<String> errors)` — lines 120-124: log info/warn.

    Each extracted method has complexity ~5-10, well below 15. The main validate() has complexity ~6 (just sequential calls with no branching).

    CRITICAL: Do not change behavior. Same error messages, same validation order, same return type.
  </action>
  <verify>
    <automated>cd /home/sebastian/traffic-simulator/backend && mvn test -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - MapValidator.java contains `private static final String ROAD_PREFIX = "Road "`
    - MapValidator.java contains `private static final String INTERSECTION_PREFIX = "Intersection "`
    - MapValidator.java contains methods: validateBasicFields, validateNodes, validateRoads, validateSpawnAndDespawnPoints, validateIntersections
    - The public validate() method body is <=20 lines (dispatcher only)
    - All 140 tests pass
  </acceptance_criteria>
  <done>MapValidator.validate() refactored into 5+ private methods with cognitive complexity <=15 each. Duplicated string literals extracted to constants. SQ-02 and SQ-07 resolved.</done>
</task>

</tasks>

<verification>
- `cd /home/sebastian/traffic-simulator/backend && mvn test -q` — all 140 tests pass
- `grep -c "assert" backend/src/test/java/com/trafficsimulator/engine/CommandQueueTest.java` — at least 2
- `grep -c "assert" backend/src/test/java/com/trafficsimulator/engine/VehicleSpawnerTest.java` — increased
- `grep "ROAD_PREFIX\|INTERSECTION_PREFIX" backend/src/main/java/com/trafficsimulator/config/MapValidator.java` — both constants present
- `grep -c "private.*validate\|private.*log" backend/src/main/java/com/trafficsimulator/config/MapValidator.java` — at least 5 helper methods
</verification>

<success_criteria>
- SQ-01: Both test files have meaningful assertions (java:S2699 resolved)
- SQ-02: MapValidator.validate() complexity reduced from 103 to <=15
- SQ-07: No duplicated "Road " or "Intersection " literals
- All 140 backend tests pass unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/15-sonarqube-quality-fixes/15-01-SUMMARY.md`
</output>
