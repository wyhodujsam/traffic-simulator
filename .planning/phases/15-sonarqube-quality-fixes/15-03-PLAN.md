---
phase: 15-sonarqube-quality-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/trafficsimulator/engine/LaneChangeEngine.java
  - backend/src/main/java/com/trafficsimulator/engine/IntersectionManager.java
autonomous: true
requirements: [SQ-04, SQ-05]

must_haves:
  truths:
    - "LaneChangeEngine has no method with cognitive complexity > 15"
    - "IntersectionManager has no method with cognitive complexity > 15"
    - "Lane change behavior is identical (MOBIL safety+incentive, zipper merge, cooldown)"
    - "Intersection transfer behavior is identical (right-of-way, roundabout yield, box-blocking, deadlock)"
  artifacts:
    - path: "backend/src/main/java/com/trafficsimulator/engine/LaneChangeEngine.java"
      provides: "Refactored lane change engine with extracted helper methods"
      contains: "isSafeLaneChange"
    - path: "backend/src/main/java/com/trafficsimulator/engine/IntersectionManager.java"
      provides: "Refactored intersection manager with extracted per-type logic"
      contains: "checkBoxBlocking"
  key_links:
    - from: "LaneChangeEngine.evaluateMOBIL()"
      to: "isSafeLaneChange(), checkGapSafety()"
      via: "method extraction"
      pattern: "isSafeLaneChange|checkGapSafety"
    - from: "IntersectionManager.canEnterIntersection()"
      to: "checkBoxBlocking()"
      via: "method extraction"
      pattern: "checkBoxBlocking"
---

<objective>
Reduce cognitive complexity in LaneChangeEngine (4 methods: 50, 37, 29, 16) and IntersectionManager (5 methods: 31, 27, 23, 19, 18) to <=15 each via method extraction and early returns.

Purpose: Resolve 9 CRITICAL SonarQube violations (java:S3776) in the two largest engine files.
Output: 2 modified Java files passing all 140 tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-sonarqube-quality-fixes/15-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor LaneChangeEngine — 4 methods above complexity limit (SQ-04)</name>
  <files>backend/src/main/java/com/trafficsimulator/engine/LaneChangeEngine.java</files>
  <read_first>backend/src/main/java/com/trafficsimulator/engine/LaneChangeEngine.java</read_first>
  <action>
    Refactor 4 methods in LaneChangeEngine to reduce cognitive complexity to <=15 each.

    **1. collectIntents() — complexity 50 -> <=15 (line 79):**

    The triple-nested loop (roads -> lanes -> vehicles) with multiple continue conditions drives complexity.

    Extract: `shouldSkipVehicle(Vehicle vehicle, Lane lane, long currentTick, long cooldownTicks)` -> boolean
    - Move lines 88-99 (stuck-behind-obstacle check, cooldown check, force/zipper bypass)
    - Returns true if vehicle should be skipped

    Extract: `evaluateBestIntent(Vehicle vehicle, Lane lane, Road road)` -> LaneChangeIntent or null
    - Move lines 102-128 (try left, try right, pick best)
    - Encapsulates the left/right evaluation + best-pick logic

    Resulting collectIntents() body: roads -> lanes -> vehicles -> if !shouldSkip -> evaluateBestIntent -> add to list. Simple triple loop with one conditional.

    **2. evaluateMOBIL() — complexity 37 -> <=15 (line 141):**

    Extract: `isSafeLaneChange(Vehicle subject, Vehicle newFollower, boolean isZipper)` -> boolean
    - Move lines 160-165 (safety criterion: new follower braking check)
    - Returns false if unsafe

    Extract: `checkGapSafety(Vehicle subject, double subjectPos, Vehicle newLeader, Vehicle newFollower, boolean isZipper)` -> boolean
    - Move lines 168-190 (gap checks for both zipper and normal modes)
    - Returns false if gap is insufficient

    Resulting evaluateMOBIL(): find neighbors -> compute accelerations -> if !isSafeLaneChange return null -> if !checkGapSafety return null -> compute incentive -> return intent. Linear flow, no nesting.

    **3. processZipperMerge() / markZipperCandidates() — complexity 29 (line 417):**

    Extract: `findClosestVehicleBehindObstacle(Lane lane, Obstacle obs)` -> Vehicle or null
    - Move lines 433-441 (inner loop finding closest vehicle behind obstacle within OBSTACLE_PROXIMITY)
    - Returns the closest slow vehicle or null

    Resulting markZipperCandidates(): roads -> lanes -> clear marks -> for each obstacle -> check interval -> findClosest -> mark. Reduced nesting.

    **4. findMergeTarget() — complexity 16 -> <=15:**

    This is only 1 over the limit. Apply early returns to flatten:
    - Convert nested if conditions to guard clauses at method start
    - Replace `if (condition) { ... long block ... }` with `if (!condition) return null;` followed by flat code

    CRITICAL: No behavior changes. Same MOBIL parameters, same safety thresholds, same zipper logic.
  </action>
  <verify>
    <automated>cd /home/sebastian/traffic-simulator/backend && mvn test -Dtest="LaneChangeEngineTest,RoadNarrowingIntegrationTest" -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - LaneChangeEngine.java contains methods: shouldSkipVehicle, evaluateBestIntent, isSafeLaneChange, checkGapSafety, findClosestVehicleBehindObstacle
    - collectIntents() method body is <=20 lines
    - evaluateMOBIL() method body is <=25 lines
    - All lane change and road narrowing tests pass
  </acceptance_criteria>
  <done>All 4 LaneChangeEngine methods refactored to cognitive complexity <=15. SQ-04 resolved.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor IntersectionManager — 5 methods above complexity limit (SQ-05)</name>
  <files>backend/src/main/java/com/trafficsimulator/engine/IntersectionManager.java</files>
  <read_first>backend/src/main/java/com/trafficsimulator/engine/IntersectionManager.java</read_first>
  <action>
    Refactor 5 methods in IntersectionManager to reduce cognitive complexity to <=15 each.

    **1. processTransfers() — complexity 31 (line 247):**

    Extract: `countWaitingVehicles(Intersection ixtn, RoadNetwork network)` -> int
    - Move lines 260-269 (iterate inbound roads, count vehicles near stop line with speed < 0.5)
    - Returns waiting count

    Extract: `transferFromInboundRoads(Intersection ixtn, RoadNetwork network, Map<String,Double> lastPlaced)` -> boolean
    - Move lines 256-277 (iterate inbound roads, check canEnter, transfer per lane)
    - Returns true if any transfer occurred

    Resulting processTransfers(): for each intersection -> countWaiting -> transferFromInbound -> update state -> checkDeadlocks. Simple loop.

    **2. canEnterIntersection() — complexity 27 (line 74):**

    Extract: `checkBoxBlocking(Intersection ixtn, String inboundRoadId, RoadNetwork network)` -> boolean
    - Move lines 97-114 (the box-blocking check: iterate outbound roads, check lane space)
    - Returns true if at least one outbound road has space

    Resulting canEnterIntersection(): signal check -> priority check -> roundabout check -> return checkBoxBlocking(). Four sequential checks, no nesting.

    **3. hasVehicleFromRight() — complexity 23 (line 195):**

    Extract: `hasWaitingVehicleOnRoad(Road road)` -> boolean
    - Move lines 209-219 (iterate lanes, check vehicle position >= threshold)
    - Takes road and threshold as parameters
    - Returns true if any vehicle is waiting

    Extract: `isApproachFromRight(double myAngle, double otherAngle)` -> boolean
    - Move lines 227-229 (angle normalization and range check)
    - Returns true if otherAngle is ~90 degrees clockwise from myAngle

    Resulting hasVehicleFromRight(): get myAngle -> for each other road -> if !hasWaitingVehicle continue -> if isFromRight return true. Simple loop with two guards.

    **4. checkDeadlocks() — complexity 19 (line 319):**

    This method is already mostly clean. Flatten with early returns:
    - Move `findLongestWaitingVehicle` + `forceTransferVehicle` calls into a helper: `resolveDeadlock(Intersection ixtn, RoadNetwork network, IntersectionState state, long currentTick)`
    - The helper handles: find victim, force transfer, update state, log warning
    - checkDeadlocks becomes: for each intersection -> get state -> guard (waiting < 2) -> guard (ticks < threshold) -> resolveDeadlock

    **5. transferVehiclesFromLane() — complexity 18 (line 373):**

    Extract: `tryTransferVehicle(Vehicle v, Intersection ixtn, String inboundRoadId, Road inboundRoad, RoadNetwork network, Map<String,Double> lastPlaced)` -> boolean
    - Move lines 386-end (pick outbound road, pick lane, compute position, check space, transfer)
    - Returns true if vehicle was transferred

    Resulting transferVehiclesFromLane(): compute threshold -> for each vehicle past threshold -> if tryTransferVehicle -> set transferred. Simple loop.

    CRITICAL: No behavior changes. Same transfer logic, same deadlock detection thresholds, same right-of-way rules.
  </action>
  <verify>
    <automated>cd /home/sebastian/traffic-simulator/backend && mvn test -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - IntersectionManager.java contains methods: countWaitingVehicles, transferFromInboundRoads, checkBoxBlocking, hasWaitingVehicleOnRoad, isApproachFromRight, resolveDeadlock, tryTransferVehicle
    - canEnterIntersection() method body is <=15 lines
    - processTransfers() method body is <=15 lines
    - All 140 tests pass
  </acceptance_criteria>
  <done>All 5 IntersectionManager methods refactored to cognitive complexity <=15. SQ-05 resolved.</done>
</task>

</tasks>

<verification>
- `cd /home/sebastian/traffic-simulator/backend && mvn test -q` — all 140 tests pass
- `grep -c "private.*should\|private.*evaluate\|private.*isSafe\|private.*check\|private.*find" backend/src/main/java/com/trafficsimulator/engine/LaneChangeEngine.java` — increased by at least 4
- `grep -c "private.*count\|private.*transfer\|private.*check\|private.*has\|private.*is\|private.*resolve\|private.*try" backend/src/main/java/com/trafficsimulator/engine/IntersectionManager.java` — increased by at least 5
</verification>

<success_criteria>
- SQ-04: All 4 LaneChangeEngine methods have cognitive complexity <=15
- SQ-05: All 5 IntersectionManager methods have cognitive complexity <=15
- All 140 backend tests pass unchanged
- No behavior changes — pure method extraction refactoring
</success_criteria>

<output>
After completion, create `.planning/phases/15-sonarqube-quality-fixes/15-03-SUMMARY.md`
</output>
