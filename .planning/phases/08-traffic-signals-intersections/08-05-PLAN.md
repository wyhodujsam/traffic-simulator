---
phase: 8
plan: 5
name: "Intersection Tests"
wave: 3
depends_on: ["08-01", "08-02", "08-03", "08-04"]
requirements: ["IXTN-01", "IXTN-02", "IXTN-03", "IXTN-04", "IXTN-07"]
files_modified:
  - "backend/src/test/java/com/trafficsimulator/model/TrafficLightTest.java"
  - "backend/src/test/java/com/trafficsimulator/engine/IntersectionManagerTest.java"
  - "backend/src/test/java/com/trafficsimulator/engine/TrafficLightControllerTest.java"
  - "backend/src/test/java/com/trafficsimulator/config/MapLoaderIntersectionTest.java"
autonomous: true
---

# Plan 08-05: Intersection Tests

## Goal

Create a comprehensive test suite covering all Phase 8 intersection logic: traffic light phase cycling, stop-line physics, vehicle transfers, box-blocking prevention, deadlock watchdog, right-of-way priority, and map loading with signal phases.

## Tasks

### Task 1: TrafficLight unit tests

<read_first>
- `backend/src/main/java/com/trafficsimulator/model/TrafficLight.java` (from Plan 08-01)
- `backend/src/main/java/com/trafficsimulator/model/TrafficLightPhase.java`
</read_first>

<action>
Create `backend/src/test/java/com/trafficsimulator/model/TrafficLightTest.java`:

```java
@Test void phaseCyclesCorrectly()
// Build 2-phase light (GREEN 30s, YELLOW 3s). Tick for 31s. Verify phase index changes to 1 (YELLOW).

@Test void phaseWrapsAround()
// Build 2-phase light. Tick for 34s (past both phases). Verify wraps back to phase 0.

@Test void isGreenReturnsTrueForCorrectRoad()
// GREEN phase with roads ["r1", "r3"]. isGreen("r1") = true, isGreen("r2") = false.

@Test void isYellowReturnsTrueForCorrectRoad()
// YELLOW phase with roads ["r1"]. isYellow("r1") = true, isGreen("r1") = false.

@Test void redForRoadNotInCurrentPhase()
// GREEN phase with roads ["r1"]. getSignalState("r2") = "RED".

@Test void replacePhasesResetsTimer()
// Tick partway through, then replacePhases. Verify phaseElapsedMs = 0, index within bounds.
```

6 tests total.
</action>

<acceptance_criteria>
- All 6 tests pass
- Tests cover: cycle advance, wrap-around, isGreen, isYellow, RED default, replacePhases
- No external dependencies (pure unit tests)
</acceptance_criteria>

### Task 2: TrafficLightController test

<read_first>
- `backend/src/main/java/com/trafficsimulator/engine/TrafficLightController.java` (from Plan 08-01)
</read_first>

<action>
Create `backend/src/test/java/com/trafficsimulator/engine/TrafficLightControllerTest.java`:

```java
@Test void advancesAllSignalIntersections()
// Network with 1 SIGNAL intersection (2-phase light) and 1 NONE intersection.
// Call controller.tick(0.05). Verify SIGNAL light's phaseElapsedMs advanced.
// Verify NONE intersection untouched.

@Test void doesNothingOnEmptyNetwork()
// Network with no intersections. tick(0.05) should not throw.

@Test void doesNothingOnNullNetwork()
// tick(0.05, null) should not throw.
```

3 tests total.
</action>

<acceptance_criteria>
- All 3 tests pass
- Controller only processes SIGNAL intersections
- Handles null/empty networks gracefully
</acceptance_criteria>

### Task 3: IntersectionManager tests — transfers and box-blocking

<read_first>
- `backend/src/main/java/com/trafficsimulator/engine/IntersectionManager.java` (from Plans 08-02, 08-03)
</read_first>

<action>
Create `backend/src/test/java/com/trafficsimulator/engine/IntersectionManagerTest.java`:

```java
// Helper: build a minimal 4-way RoadNetwork with SIGNAL intersection, 4 inbound, 4 outbound roads.

@Test void stopLinesGeneratedForRedLight()
// Set light to RED for north/south. computeStopLines should return entries for those lanes.

@Test void noStopLinesForGreenLight()
// Set light to GREEN for all. computeStopLines should return empty map.

@Test void vehicleTransferredOnGreenLight()
// Place vehicle at end of inbound road. Light is GREEN. processTransfers should move it to an outbound road at position 0.

@Test void vehicleBlockedOnRedLight()
// Place vehicle at end of inbound road. Light is RED. processTransfers should NOT move it.

@Test void boxBlockingPreventsEntry()
// Light is GREEN, but outbound road's first vehicle is at position 2.0 (< MIN_ENTRY_GAP).
// Vehicle should NOT be transferred.

@Test void boxBlockingAllowsWhenOutboundClear()
// Light is GREEN, outbound road is empty. Vehicle SHOULD be transferred.

@Test void vehicleSpeedPreservedDuringTransfer()
// Vehicle at speed 10.0 transferred. After transfer: speed still 10.0, position = 0.0.

@Test void deadlockDetectedAndResolved()
// All 4 approaches have waiting vehicles, all outbound roads full.
// Tick processTransfers 201 times without any transfer.
// After 201st: one vehicle should have been force-advanced.

@Test void rightOfWayYieldsToVehicleFromRight()
// PRIORITY intersection with 2 perpendicular inbound roads.
// Vehicle approaching from the right road. Our road should NOT be allowed to enter.
```

9 tests total.
</action>

<acceptance_criteria>
- All 9 tests pass
- Tests cover: stop lines, transfers, red light blocking, box-blocking, speed preservation, deadlock, priority
- Each test builds a focused RoadNetwork with minimal setup
- Tests are independent (no shared mutable state)
</acceptance_criteria>

### Task 4: MapLoader intersection loading test

<read_first>
- `backend/src/main/java/com/trafficsimulator/config/MapLoader.java`
- `backend/src/main/resources/maps/four-way-signal.json`
</read_first>

<action>
Create `backend/src/test/java/com/trafficsimulator/config/MapLoaderIntersectionTest.java`:

```java
@Test void loadsFourWaySignalMap()
// Load four-way-signal.json. Assert:
// - 8 roads
// - 1 intersection with id "n_center"
// - intersection has 4 inbound roads, 4 outbound roads
// - intersection has a TrafficLight with 6 phases
// - 4 spawn points, 4 despawn points

@Test void trafficLightPhasesMatchConfig()
// Load four-way-signal.json. Assert:
// - Phase 0: GREEN, greenRoadIds contains "r_north_in" and "r_south_in"
// - Phase 1: YELLOW, same roads
// - Phase 2: ALL_RED, empty greenRoadIds
// - Phase 3: GREEN, greenRoadIds contains "r_west_in" and "r_east_in"

@Test void straightRoadStillLoadsCorrectly()
// Load straight-road.json. Assert: 1 road, 0 intersections, 3 spawn points.
// Regression check that existing maps are not broken.
```

3 tests total.
</action>

<acceptance_criteria>
- All 3 tests pass
- `four-way-signal.json` loads correctly with all intersection wiring
- `straight-road.json` backward compatibility confirmed
- Total new tests in Phase 8: 21 (6 + 3 + 9 + 3)
- All existing tests still pass (no regressions)
</acceptance_criteria>
